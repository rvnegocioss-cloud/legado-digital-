# Legado Digital — Briefing do Projeto

> Histórico detalhado (bug corrigido, decisão tomada, mudança dia-a-dia) arquivado no vault: `Projects/Legado Digital - Historico Detalhado.md`. Este arquivo mantém só regra permanente + estado atual + arquitetura de referência — carregado toda sessão, precisa ficar enxuto.

## Context Navigation (Graphify)

Grafo de código local em `graphify-out/graph.json` (1421 nodes, 2580 edges, 108 comunidades, gerado 100% local via tree-sitter, `--code-only`, zero API). Antes de explorar estrutura/relação entre arquivos, consultar o grafo primeiro (`/graphify` ou `graphify query "<pergunta>"`) em vez de ler arquivo cru — só ler o arquivo de verdade quando for editar ou o grafo não tiver a resposta. Regenerar depois de mudança grande: `graphify update .` (sem custo de API). `graphify-out/` fora do git (`.gitignore`) e fora do contexto de sessão (`.claudeignore`) — é cache regenerável, não fonte de verdade.

## Regras Permanentes (nunca ignorar)

1. **Proteção contra ação destrutiva** — nunca `DROP TABLE`/`DELETE` sem filtro/`TRUNCATE`/migração que altera ou remove coluna existente/`git push --force`/`git reset --hard`/deletar branch/`rm -rf` sem mostrar a ação exata primeiro e ter confirmação explícita do Rafael. Backup automático obrigatório (`scripts/backup-supabase.js`, salva em `Desktop\Cerebro Claude - Legado Digital\backups\`) antes de qualquer ação destrutiva. Supabase do projeto é plano free, sem PITR — motivo a mais pra nunca arriscar sem backup.
2. **Tudo escalável desde o início** — schema de banco, upload de arquivo, geração de identificador, rate limit: nunca gambiarra que só funciona em escala pequena, mesmo que "resolva por agora".
3. **Modelo por tarefa** (Agent tool) — Sonnet 5: execução (código/edição/build/commit), padrão dessa sessão, não precisa spawnar subagente pra isso. Opus 4.8: planejamento complexo/arquitetura, `subagent_type:"Plan"` + `model:"opus"` antes de decisão grande — nunca inventar arquitetura complexa sozinho. Haiku 4.5: tarefa mecânica/repetitiva de subagente. Fable 5: teto de capacidade quando a tarefa realmente precisa (caro, 1M contexto), ainda não testado no projeto.
4. **CLAUDE.md atualizado no mesmo commit** da feature, nunca depois. Marcar `[x]`, atualizar Fase Atual/O que está pronto. Se uma decisão anterior for **revertida/mudada** no meio da sessão (ex: um fluxo que foi construído e depois desfeito a pedido do Rafael), a descrição velha tem que ser corrigida na hora, não pode sobreviver até o Rafael notar depois. Changelog detalhado (o "como/por que" de cada mudança) vai pro vault (Dev Logs/), não empilha aqui — aqui fica só a linha resumida. **Antes de encerrar a sessão ou trocar de sessão**, revisar o arquivo inteiro contra o que foi feito de verdade (não só os últimos commits) — item desatualizado não pode esperar o Rafael perguntar. Regra reforçada 2026-07-29.
5. **Backup de material relevante** (PDF/doc/técnica validada) em `Desktop\Cerebro Claude - Legado Digital\` (HTML simples, sem base64/Drive).
6. **Pesquisar referência real antes de construir UI/efeito com barra de qualidade alta** (CodePen/Awwwards/doc oficial) — nunca inventar do zero e torcer pra ficar bom.
7. **Build passou → commit + push direto, sem perguntar.** Só para e avisa se o build falhar.
8. **Nunca pedir slug/endereço técnico em tela pública** — busca sempre por nome do homenageado, slug resolvido por trás.
9. **Log de skill/MCP usado** em `docs/USO_SKILLS_MCPS.md` (o quê, motivo, o que produziu).
10. **Rótulo de UI deixa ação óbvia** (ex: "Memoriais (Cadastrar/Editar)", não só "Memoriais").
11. **Toda página tem link de volta visível no topo** (nunca só o botão voltar do navegador) — página de detalhe/edição/login, e também **todo portal logado (Central, Parceiro, Família) precisa de "Voltar pro site" apontando pra landing**, visível em qualquer largura de tela. Regra ampliada 2026-09-08 depois de achado real: o Portal do Parceiro e o da Família não tinham nenhum, e o da Central existia só no celular (`md:hidden`), sumindo no desktop.
12. **Logo real (`public/logo-legado-digital.svg`) em TODA página pública, não só telas de acesso/identificação** — via `next/image`, nunca texto solto (`<span>Legado Digital</span>` não vale). Sempre menor que o container que envolve, com folga — nunca transborda. Toda página pública também leva rodapé com link pra Privacidade e Termos. Regra ampliada 2026-08-28 depois de achado real: `/parceiros/[slug]`, `/busca`, `/cemiterios`, `/cemiterios/[cidade]` e `/cemiterios/[cidade]/[cemiterio]` tinham só texto no rodapé em vez do logo — corrigido nas 5.
13. **Layout de ficha na Central** — card grande (form/tabela/lista que cresce) ocupa linha cheia ou split 2/3+1/3 com painel de referência curto ao lado (caso "um grande + um de consulta rápida"); cards curtos nunca ficam sozinhos numa coluna larga, vão lado a lado num grid parelho (`lg:grid-cols-2 xl:grid-cols-3`). Central é ferramenta de trabalho densa (tela larga aproveitada), diferente do lado público (mobile-first).
14. **O LegadoBot sabe de TUDO, sempre — regra reforçada pelo Rafael em 2026-09-04.** `docs/LEGADOBOT_PROMPT.md`/`LEGADOBOT_PROMPT_PUBLICO.md` atualizados junto com **qualquer** mudança no sistema — não só Central/Portal do Parceiro/segurança, mas também landing, telas de acesso, página pública, Portal da Família, fluxo novo, tabela nova, rota nova. Se mudou algo que alguém pode perguntar pro bot, o prompt muda no mesmo commit. Nunca deixar pra depois, nunca esperar ser lembrado. Vale pra mudança relevante em — **automático, sem esperar aprovação** (mudou 2026-07-30 a pedido do Rafael: o bot precisa sempre saber do estado atual do sistema). Vale junto a regra 22 (hierarquia de segurança) — toda mudança de prompt tem que respeitar o corte estrutural staff/parceiro, nunca só instrução de texto. **Falha real, corrigida 2026-08-07**: a leva inteira de features de mapa/cemitério/parceiro daquele dia (várias sessões seguidas) não atualizou o prompt nenhuma vez — só percebido quando o Rafael perguntou direto se o bot sabia. Checklist mental antes de marcar qualquer tarefa como concluída: se mexeu na Central ou no Portal do Parceiro, checar se `LEGADOBOT_PROMPT.md` precisa da mesma atualização, no mesmo commit — não depois, não só quando lembrado.
15. **Ideia/pergunta sem resposta do Rafael** vai pro `docs/RASCUNHO_IDEIAS.md` — nunca decide/constrói de lá sem confirmação dele primeiro.
16. **Toda feature nova refletida nos lados relevantes** (Central ↔ Portal do Parceiro ↔ Página Pública) — nunca implementar só de um lado.
17. **Nunca remover/mexer na seção "Como Chegar"** (`GuiaTumulo`/`GuiaTumuloCarregador`, mapa/rota até o cemitério e o túmulo na página do memorial) sem pedido explícito do Rafael. Regra registrada 2026-07-23.
18. **Não desobedecer instrução explícita do Rafael** — quando ele pede uma ação direta (commitar, subir, corrigir algo específico), executar sem ficar re-explicando/re-verificando em loop nem adicionando passo extra não pedido. Dúvida real de segurança (regra 1) ainda vale, mas fora isso a obediência é direta. Regra registrada 2026-07-24.
19. **Quando faltar dado pra uma seção aparecer** (ex: memorial de teste sem galeria/timeline), gerar/cadastrar o dado direto (fictício quando for teste, real quando fizer sentido) — não só reportar que falta, resolver. Regra registrada 2026-07-24.
20. **Nunca mexer na estrutura/posição da vela (`components/public/AcenderVela.tsx`)** sem pedido explícito — corpo da vela, pavio, posição da chama (`bottom: calc(100% + 14px)`, `left: calc(50% + 8px)`, técnica `border-radius: 50% 50% 50% 0` + `rotate(-45deg)`) já estão calibrados certos, deu muito trabalho alinhar. Se qualquer variação nova de layout for criada, essa seção entra **idêntica**, reaproveitando o componente sem alteração. Regra registrada 2026-07-24.
21. **A página atual do memorial (`/homenagem/[slug]`) é o modelo base — nunca reescrita.** Qualquer variação de layout nova é uma página/rota separada, nunca substitui ou modifica a atual — **a não ser por decisão explícita do Rafael de promover uma variante**, como aconteceu em 2026-08-27 (a variante que era `/perfil` — livro de assinaturas, régua lateral, castiçal — virou a `/homenagem/[slug]` oficial; o design antigo foi preservado intocado em `/homenagem/[slug]/classico`, nunca apagado). Ponto de restauração da virada: tag git `pagina-classica-antes-da-virada-2026-08-27`. O design anterior a essa virada tinha sua própria tag, `homenagem-modelo-base-2026-07-24`, e print de referência em `docs/pagina-atual-referencia-2026-07-24.png`. **Detalhe completo dos modelos travados (página oficial + página clássica preservada + vela) em `docs/MODELOS.md`** — consultar ali antes de qualquer dúvida, nunca editar o que está descrito lá sem pedido explícito. Regra registrada 2026-07-24, atualizada 2026-08-27.
22. **Hierarquia de segurança entre os 3 níveis nunca se cruza** — Central (staff) acessa tudo → Parceiro B2B só os próprios memoriais/dados → Família só o próprio memorial (depois de e-mail+senha). Parceiro nunca acessa nada equivalente à Central; Família nunca acessa Parceiro nem outro memorial. Vale pra toda feature nova: RLS/policy, API route, e agora também o LegadoBot (prompt cortado por papel — a seção só-staff é removida do contexto do modelo pra parceiro, não é só instrução de "não conte", ver regra 14). Qualquer mudança de schema/rota precisa checar contra essa régua antes de considerar pronta. Regra registrada 2026-07-30 depois de auditoria real (Opus) que confirmou o desenho correto mas achou 1 furo crítico + 8 médios/baixos, todos corrigidos no mesmo dia.

23. **Nada de convenção inventada em assunto que tem norma própria.** Antes de construir qualquer coisa que segue regra estabelecida fora do projeto (árvore genealógica, hierarquia cemiterial, símbolo religioso, documento legal, norma de acessibilidade), **pesquisar a convenção real primeiro** e registrá-la junto do código — nunca deduzir pelo que "parece certo". Regra registrada 2026-09-08 depois de eu montar uma árvore genealógica só pela leitura visual (ancestral embaixo, descendente em cima), sem linha de parentesco, sem casal unido, sem lado paterno/materno e sem ordem de nascimento — o Rafael pegou na hora: "está aplicando as regras de uma árvore genealógica comprovada?". **Convenções da genealogia tradicional, já pesquisadas e válidas pro projeto:** pai à esquerda e mãe à direita (padrão brasileiro, lado paterno sempre à esquerda); linha horizontal liga pessoas da mesma geração (casal); linha vertical liga pais a filhos; linha sólida é casamento, pontilhada é divórcio/separação; irmãos em ordem de nascimento, do mais velho à esquerda; a pessoa de referência começa no centro ou na base. Fontes: [MyHeritage](https://education.myheritage.com.br/article/how-to-make-a-family-tree-step-by-step-guide-online-tools/), [Lucidchart](https://www.lucidchart.com/blog/pt/como-fazer-uma-arvore-genealogica), [Genera](https://www.genera.com.br/blog/arvore-genealogica/).
24. **Toda refatoração visual passa por wireframe (skill `opendesign-wireframe`) antes de mexer em código.** Gerar 3-5 opções estruturalmente diferentes, o Rafael escolhe uma, só então codar. Nunca aplicar direto uma mudança de layout/reorganização visual sem mostrar opção antes — regra registrada 2026-09-15 depois de eu ter tentado aplicar direto pelo menos 2 reorganizações (proporção do card do topo, mosaico de fotos) sem mostrar alternativa, o Rafael rejeitou as duas e teve que pedir o wireframe explicitamente, repetindo o pedido várias vezes com raiva. Vale pra reorganização/reestruturação de layout já existente — não pra ajuste pontual óbvio (cor, texto, bug de espaçamento simples) nem pra primeira construção de algo que ainda não existe na página (aí a brainstorming/frontend-design normal decide o processo).

## Convenções
- **Todo campo de busca e de preenchimento completa enquanto a pessoa digita** (regra do Rafael, 2026-09-15) — nunca exigir clique num botão "Buscar", nunca `<select>` que carrega a entidade inteira de uma vez. Usar `lib/useBuscaDebounce.ts` (espera 300ms, mínimo 2 caracteres, descarta resposta de busca antiga) e, pra escolher jazigo, o `components/admin/SelecaoJazigo.tsx`. `<select>` continua valendo só pra vocabulário fixo e curto (parentesco, situação, tipo).
- Toda área de cadastro nova vem com 2 registros fictícios já cadastrados (nunca tela vazia ao revisar).
- Todo campo de formulário tem `<label>` descritivo acima, nunca só placeholder.
- Sem emoji como ícone de UI — usar `lucide-react`, sempre `strokeWidth={1.5}`, cor neutra.
- Limite de upload/quantidade sempre visível no label do campo (ex: `Galeria de fotos (2/4)`).

## O que é
Plataforma B2B2C para criação, gestão e acesso a memoriais digitais vinculados a QR Codes, lápides, jazigos, gavetas, caixas ossuárias, cemitérios, crematórios, funerárias, planos funerários, prefeituras e concessionárias cemiteriais. Famílias preservam histórias, fotos, vídeos, mensagens e registros de pessoas falecidas, com privacidade/governança definida pelos próprios familiares.

## Modelo de Negócio
B2B2C — parceiros privados e públicos como canais de venda, ativação e operação. **Confirmado em ata de reunião de sócios (16/07/2026, `docs/ATA_REUNIAO_SOCIOS.md`): não é venda direta pra família.** Funerárias oferecem o Legado Digital como benefício adicional; monetização via taxa anual integrada aos planos já existentes da funerária.

## Stack Técnica
- Frontend/Backend: Next.js 16 + TypeScript (App Router)
- Banco: Supabase (PostgreSQL) · ORM: Prisma
- Estilo: Tailwind CSS v4 + shadcn/ui
- Deploy: Vercel · Auth: Supabase Auth (email/senha no MVP) · Storage: Supabase Storage

## Os 6 Ambientes do MVP
1. **Website institucional** — captação de parceiros B2B
2. **Admin Legado Digital** (Central) — equipe interna opera tudo
3. **Portal do parceiro B2B** — cemitérios, funerárias, prefeituras
4. **Portal da família** — familiares gerenciam o memorial
5. **Página do memorial** — visitantes acessam via QR Code, URL ou busca
6. **Busca pública** — busca de memoriais com filtros e privacidade

## Parceiros B2B Atendidos
- Empresas privadas: cemitérios, crematórios, funerárias, planos funerários
- Empresas públicas: prefeituras, cemitérios municipais, autarquias, concessionárias
- Parceiros comerciais: associações, entidades religiosas, canais de venda regionais

## Estrutura Cemiterial (hierarquia obrigatória)
```
País → Estado → Cidade
  → Cemitério / Crematório (lat/lng recomendado)
    → Setor / Quadra / Ala / Bloco (lat/lng opcional)
      → Jazigo / Túmulo / Lápide (lat/lng opcional)
        → Gaveta (herda lat/lng do jazigo)
          → Pessoa falecida → Memorial
        → Caixa ossuária (herda lat/lng do jazigo)
          → Pessoa exumada → Memorial
```
Um jazigo pode ter várias gavetas. Cada gaveta pode conter uma pessoa. Um jazigo também pode conter caixas ossuárias de pessoas exumadas.

## Papéis de Usuário
| Papel | Função |
|---|---|
| Admin Legado Digital | Gestão total da plataforma |
| Operador Legado Digital | Cadastro e edição operacional |
| Parceiro B2B | Acompanha memoriais, solicita ativações, acessa QR Codes |
| Familiar responsável | Aceita termos, administra memorial, define privacidade |
| Familiar administrador | Ajuda na gestão conforme permissões |
| Visitante identificado | Acessa quando memorial exige login |
| Visitante público | Visualiza conteúdos permitidos |

## Modos de Privacidade do Memorial
2 eixos ortogonais: **canal** (busca / link direto / QR Code — os 3 toggles independentes) × **portão** (`modo_gate`: aberto · com senha · com identificação/cadastro · lista de e-mails autorizados · oculto). Todos os 5 modos de portão implementados (2026-07-31) — detalhe completo na Fase Atual.

## Entidades Principais do Banco
usuarios, perfis, permissoes · parceiros_b2b, contratos, pagina_publica_parceiro · planos, aquisicoes, utilizacao, fechamento_mensal · paises, estados, cidades · cemiterios, crematórios, setores, quadras, lapides, gavetas · caixas_ossarias · pessoas, homenagens (memoriais), responsaveis_familiares · fotos, videos, documentos, historias, condolencias, mural_memorias · qr_codes, homenagens_seguranca, termos_aceite, notificacoes

## Fluxo Operacional do MVP
1. Parceiro B2B apresenta e vende o serviço
2. Parceiro ou equipe Legado Digital registra solicitação inicial
3. Familiar responsável aceita termos e fornece dados mínimos
4. Equipe ou familiar complementa dados, conteúdo e localização
5. Memorial criado em modo rascunho
6. Família revisa, aprova privacidade e autoriza publicação
7. QR Code e URL gerados e vinculados à estrutura física
8. Memorial publicado conforme regra de acesso definida
9. Interações futuras geram notificações aos responsáveis
10. Utilização registrada para fechamento mensal e futura integração ERP

## Roadmap — Fases
| Fase | Entrega |
|---|---|
| **Fase 1** ✅ | Fundação: banco, auth, admin básico, website inicial |
| **Fase 2** (em andamento) | Portal parceiro, busca, página pública do parceiro |
| **Fase 3** | Portal família, conteúdo, privacidade, publicação |
| **Fase 4** | Planos, aquisições, utilização, fechamento mensal |
| **Fase 5** | Geolocalização avançada, mapeamento cemiterial |

## Mapa Visual das Páginas
Organograma dos 6 ambientes + fluxo de dados dos memoriais (`parceiro_id`): mantido como Artifact em `/admin/mapa`, atualizar a cada mudança estrutural relevante. **Em abas desde 2026-09-17** (mesmo padrão da ficha do memorial/jazigo — página tinha 5 seções empilhadas, ~900 linhas de rolagem): `Ambientes · Módulos da Central · Fluxo do Memorial · Segurança · Sugestões`, abre em Ambientes. Aviso "Manual do sistema →" e o rodapé "Atualizado em..." ficam fixos, fora das abas.

## Fase Atual
**FASE 1 concluída → FASE 2 em andamento (Portal parceiro)**

Tudo do checklist de Fase 1/2 abaixo está **feito** (schema, auth, CRUDs de Parceiros/Cemitérios/Memoriais, Portal do Parceiro, Portal da Família, busca pública, senha de acesso/edição, timeline, QR Code automático + e-mail pro fornecedor, Central de Comunicações, lápide/gaveta + visualização 3D, rate limit middleware, política de privacidade/termos, landing no padrão visual, LegadoBot Fase 1 teste) — detalhe item-a-item de cada um no vault (`Projects/Legado Digital - Historico Detalhado.md`).

**Pendente (backlog ativo):**

- [x] **Informações do cemitério dentro da seção "Sobre", abaixo do mapa** (2026-09-23, pedido do Rafael: endereço completo, contato e o que mais fosse relevante, pesquisado na internet) — endereço (com bairro), horário de visitação e telefone ficam **na mesma grade de 3 colunas** da seção "Sobre o cemitério", abaixo do mapa, com os mesmos títulos dourados e a mesma fonte de `Serviços`/`Site oficial`: `Endereço · Visitação · Contato` numa linha, `descrição · Serviços · Site oficial` na outra, **sem linha divisória entre elas**. O telefone abre o discador; WhatsApp, quando existir, abre `wa.me`. **Quatro versões foram reprovadas pelo Rafael antes desta** (nesta ordem): ficha em caixa com borda ao lado do botão "Caminho até o cemitério" → mesma ficha sem moldura → bloco solto abaixo do mapa com tipografia própria (`dl/dt/dd`) → duas grades separadas por uma linha. O que ele cobrou nas quatro foi a mesma coisa: **espaço morto ao lado do endereço e tipografia fora do padrão do site.** A ficha separada não existe mais. Campo sem dado **não aparece** (nunca "—"). **Banco (colunas novas em `cemiterios`, todas nulas por padrão, nada existente mudou):** `bairro`, `horario_visitacao`, `descricao_publica`, `site_url`, `servicos text[]`, `contatos jsonb` (lista de `{rotulo, tipo: telefone|whatsapp, valor}`), `informacoes_fonte`, `informacoes_atualizadas_em` — migrations `20260923_cemiterios_informacoes_publicas.sql` e `20260923_mapa_publico_informacoes.sql` (função `obter_mapa_publico_cemiterio` devolve os campos no objeto `cemiterio`; ACL conferido idêntico; rollback = reaplicar `20260923_mapa_publico_foto_lapide.sql`); backup completo antes (`legado-digital-2026-09-23T16-46-31.sql`, 29/29 tabelas). **Por que `contatos` é lista com rótulo e não uma coluna `whatsapp`:** o único WhatsApp encontrado é o **da Prefeitura (SIM, consulta de sepulturas), não o do cemitério** — a página precisa dizer de quem é o número em vez de fingir que é do cemitério. **Cadastro na Central** (`/admin/cemiterios`) ganhou os campos (contatos em linhas com rótulo, serviços um por linha, "de onde vieram" — a data de conferência é gravada sozinha). **Dados reais do São Pedro (pesquisados, com fonte gravada):** Av. Paes Leme, 855, bairro Martins; visitação 7h às 17h30; cemitério (34) 3219-7017; Secretaria de Serviços Urbanos (34) 3239-2451 / 3239-2816 (seg–sex 12h–17h); WhatsApp da Prefeitura (34) 3239-2800 (consulta de sepulturas, opção 8) e site `sepultos.uberlandia.mg.gov.br`; 2 salas velatórias (reformadas em 2024, uma com serviço póstumo gratuito p/ famílias em vulnerabilidade); o mais antigo da cidade. Fontes: [Prefeitura — Serviços Póstumos e Cemitérios](https://www.uberlandia.mg.gov.br/prefeitura/secretarias/secretaria-de-servicos-urbanos/servico-de-luto/), [consulta de sepulturas pelo WhatsApp do SIM](https://www.uberlandia.mg.gov.br/2025/10/30/prefeitura-disponibiliza-consulta-de-localizacao-de-sepulturas-nos-cemiterios-de-uberlandia-pelo-whatsapp-do-sim/), [reforma das salas velatórias](https://www.uberlandia.mg.gov.br/2024/01/10/prefeitura-conclui-reforma-das-salas-velatorias-do-cemiterio-sao-pedro/), [Museu Virtual de Uberlândia](https://www.museuvirtualdeuberlandia.com.br/cemiterio-sao-pedro-museu-a-ceu-aberto/). **Não usado de propósito:** data de fundação (as fontes divergem, 1928 x 1939) e o bairro "Osvaldo Rezende" (aparece em resumo de terceiro; a Prefeitura diz Martins). O site da Prefeitura bloqueia acesso automático (403), então os números vieram de trechos indexados do próprio domínio oficial — **vale conferir por ligação antes de divulgar.** Ainda sem WhatsApp próprio do cemitério: cadastrar quando houver. **Os outros dois cemitérios públicos também foram pesquisados e preenchidos (2026-09-23):** **Bom Pastor** (Uberlândia; nome oficial "Cemitério Municipal Campo do Bom Pastor") — Av. Gameleiras s/nº, bairro Jaraguá, 7h às 17h30, telefone (34) 3238-2930, mesmos contatos da Secretaria/SIM do São Pedro, obras de ampliação e reforma iniciadas em set/2025 ([Prefeitura](https://www.uberlandia.mg.gov.br/2025/09/22/prefeitura-inicia-obras-de-ampliacao-e-reforma-do-cemiterio-campo-do-bom-pastor/)); **José Lázaro** (Tupaciguara) — só o confirmável: municipal, mantido pela Prefeitura (Secretaria de Meio Ambiente, Recursos Hídricos e Serviços + Obras), passou por ampliação ([AMVAP](https://www.amvapmg.org.br/1/ampliacao-do-cemiterio-municipal-foi-inaugurada/)), telefone da Prefeitura (34) 3281-0000 ([tupaciguara.mg.gov.br](https://www.tupaciguara.mg.gov.br/fale-conosco/)); **horário e telefone próprio do José Lázaro não foram encontrados e ficam em branco** (não invento). O nome do Bom Pastor no banco está `bom pastor ` (minúsculo, com espaço) — não renomeei sem pedido. O bloco lateral do "Sobre" virou **"Site oficial"** genérico: a 1ª versão dizia "Consultar sepultados", o que só é verdade em Uberlândia e seria informação falsa em Tupaciguara. **Contatos reduzidos a um por cemitério (2026-09-23, pedido do Rafael: "pra que aquele tanto de telefone lá, coloca só do cemitério"):** ficaram só (34) 3219-7017 no São Pedro e (34) 3238-2930 no Bom Pastor; saíram os dois da Secretaria de Serviços Urbanos e o WhatsApp do SIM, que esticavam a coluna e criavam o vão ao lado do endereço. O José Lázaro segue com o telefone da Prefeitura de Tupaciguara, **o único número público que ele tem**. Os contatos apagados estão salvos em `Desktop\Cerebro Claude - Legado Digitalackups\contatos-cemiterios-antes-2026-09-23.sql` (é só rodar o arquivo pra ter de volta). Com um contato só, o rótulo ("Cemitério") some — repetia o título da coluna. A linha "Fonte:" continua citando o WhatsApp do SIM porque foi de lá que a informação veio, mesmo o número não aparecendo mais.

- [x] **Card de jazigo familiar no mapa público, no padrão do card da Central** (2026-09-23, pedido do Rafael) — túmulo com **2 ou mais homenageados** abre o card de jazigo familiar (`components/public/CardPino.tsx`, CSS `.card-pino-publico`/`.cp-*` em `globals.css`). **Estrutura final (2026-09-23, depois de 3 reprovações do Rafael: "diminui essa merda", "não está igual ao card menor", "olha a diferença de design e tamanho"):** o card de jazigo ficou **idêntico ao card de 1 memorial** — mesma largura (**176px**, era 210px), mesma fonte, mesmos nodes (**24px**, eram 32px). **A foto da lápide saiu do card** e o "JAZIGO FAMILIAR" em caixa alta virou só o nome do jazigo no topo, sobre uma linha fina. Os homenageados ficam em **lista vertical, um abaixo do outro, com a foto dentro de um NODE** (círculo com anel dourado `conic-gradient`, o mesmo da Central e da landing) e o nome ao lado, sublinhado dourado, levando ao memorial. Sem gaveta (G1, G2…) e sem controles de edição: visitante não vê gaveta nem ossário. Com 1 memorial segue o card simples (node + nome + "Toque para ver o memorial"). Vale nos dois mapas públicos (página do cemitério e landing, mesmo `MapaPublicoCemiterio`). **A 1ª versão errou e foi refeita no mesmo dia:** usava retratos retangulares 4:5 lado a lado e ficava grande demais — o padrão do projeto é o node redondo em lista vertical; o Rafael mandou olhar o card da Central em vez de inventar. **O card não pode sumir ao chegar nele:** antes fechava no instante em que o mouse saía da cruz (`onMouseMove` sem feature → `setHover(null)`), então no computador ninguém alcançava os nomes; agora fica enquanto o mouse está na cruz **ou** no card e fecha 400ms depois de sair dos dois (`agendarFechar`/`cancelarFechar`); clicar numa cruz de jazigo fixa o card (✕ fecha) e a cruz é empurrada pra baixo (`easeTo offset`) pra sobrar espaço em cima; o popup escolhe a âncora sozinho (abre pra baixo quando a cruz está no alto). **Privacidade:** memorial com senha mostra nome + cadeado no lugar da foto; oculto/sem link/sem busca/rascunho nunca entram (filtro da RPC inalterado). **2 bugs meus achados testando em produção e corrigidos:** a lista lateral "Memoriais neste cemitério" mostrava **um item por túmulo** (jazigo com 2 pessoas mostrava só a primeira, cabeçalho contava 2 memoriais quando eram 3) — agora um por pessoa; e a foto de quem vem **aninhado** em `properties.memoriais` chegava crua do balde privado (imagem quebrada) — agora assinada (`urlMidiaProtegida`, idempotente). **Banco (só função, nenhuma tabela):** `obter_mapa_publico_cemiterio` ganhou `foto_lapide` (`lapides.foto_face_url`) — migration `20260923_mapa_publico_foto_lapide.sql`, ACL conferido idêntico depois (só anon/service_role), rollback em `Desktop\Cerebro Claude - Legado Digitalackups
ollback-obter_mapa_publico_cemiterio-2026-09-23.sql`, backup completo antes (`legado-digital-2026-09-23T15-35-00.sql`). **Validado no navegador em produção** (hover na cruz abre, mouse chega no card e ele fica, link leva ao memorial, tirar o mouse fecha). **Dados de teste FICTÍCIOS no ar (regra 19):** túmulo `Q36-R01-T001` do São Pedro (outra ponta da fileira do Carlos Saraiva) virou **"Jazigo Família Costa Lima"** com **Antônio Ferreira Lima** (gaveta 1) e **Helena Martins Costa** (gaveta 2), memoriais de teste que já existiam sem vínculo; túmulo `confirmada` (pino verde na Central) com foto de lápide e retratos. **Retratos trocados em 2026-09-23** pelos do acervo do próprio projeto (`Documents\Desktop Antigo\legado imagens\extras-comfyui\ComfyUI_00042_.png` e `00043_.png`, o senhor e a senhora), a pedido do Rafael — antes eram ilustrações que eu tinha gerado em Python só pra validar enquadramento, porque o ComfyUI estava fora do ar. A foto da lápide do jazigo segue sendo ilustração. **Como o mapa é público, os fictícios aparecem nele até o Rafael mandar apagar.** **Ajustes do Rafael no mesmo dia (depois de ver no ar):** o botão "Tela cheia" ficava no canto de cima do mapa e **tapava a lista de resultados da busca** — agora mora no **rodapé do mapa** (em tela cheia, onde o rodapé some, fica no canto de baixo); a foto da lápide **cabe inteira** no card (largura toda, altura proporcional, teto de 150px, `object-fit: contain`) em vez da faixa fixa de 84px copiada da Central, que cortava a foto ao meio; o texto **"Mapa aéreo de drone"** saiu dos cards de cemitério (`/cemiterios/[cidade]`) e da frase do card da landing — ninguém pediu isso, veio do código original e eu mantive sem perguntar (o crédito pequeno "Ortomosaico de drone · Legado Digital" no canto do mapa e o texto do `GuiaTumulo` seguem, este último por causa da regra 17). Ficou de fora: página `/jazigo/[slug]` e o `noindex` da página do cemitério.
ollback-obter_mapa_publico_cemiterio-2026-09-23.sql`, backup completo do banco antes (`legado-digital-2026-09-23T15-35-00.sql`). **Dados de teste FICTÍCIOS no ar (regra 19):** túmulo `Q36-R01-T001` do São Pedro (outra ponta da fileira do Carlos Saraiva) virou **"Jazigo Família Costa Lima"** com **Antônio Ferreira Lima** (gaveta 1) e **Helena Martins Costa** (gaveta 2), ambos memoriais de teste que já existiam sem vínculo; túmulo `confirmada` (pino verde na Central) com foto de lápide e retratos. **As 3 imagens são ilustrações geradas em Python, não fotos** (ComfyUI estava fora do ar) — servem só pra validar enquadramento; **como o mapa é público, os fictícios aparecem nele até o Rafael mandar apagar.** Ficou de fora: página `/jazigo/[slug]` e o `noindex` da página do cemitério.

- [x] **Navegação de retorno corrigida: "voltar" volta pra onde a pessoa veio** (2026-09-23, prioridade do Rafael) — achado real: clicou na cruz do mapa do cemitério, abriu o memorial, apertou a seta ao lado da logo e caiu na **landing** em vez de voltar pro mapa. Causa: a decisão "veio de fora do site?" usava `document.referrer`, que é fixado no carregamento inicial da aba e **não muda em navegação interna do Next** — quem abria o mapa direto ficava com referrer vazio pra sempre e todo "voltar" virava "ir pra home". Além disso, a seta do `SiteNav` (que eu tinha devolvido) era um link fixo pra `/`, sem histórico nenhum. **Correção:** `lib/useVoltar.ts` (hook único) decide por `navigation.canGoBack` (Navigation API — só enxerga entradas do MESMO site, então página anterior do Google/WhatsApp não conta e a pessoa não é jogada pra fora; fallback `history.length > 1` em navegador sem a API); sem página anterior no site (QR Code, link direto) cai no pai lógico da página. Usado em **todo** "voltar": seta do `SiteNav`, seta do memorial (`VoltarLink.tsx`) e os "← Voltar" das páginas de cemitério (`components/public/VoltarPagina.tsx`, mantém o `href` real — clique do meio/nova aba/leitor de tela seguem funcionando; rótulo virou "← Voltar" simples porque agora pode voltar pra mapa, busca ou landing). **Seta, logo e "Voltar pro site" agora são 3 controles separados** (antes seta e logo eram o mesmo link): seta = página anterior, logo = home, texto "Voltar pro site" = home explícito (regra 11) — nenhum dos três aparece na home (a pessoa já está lá). Botão "Voltar pro site" também entrou na nav do memorial. Os destinos explícitos ("Voltar pro site", "Voltar pro login", "Voltar pro memorial", modais) ficaram como estavam — são o que dizem. **Pesquisa (regra 23), mesmo dia:** NN/g — migalha mostra a **hierarquia** do site, não o histórico, e não deve duplicar o botão voltar; Baymard — o voltar tem que levar à página que a pessoa *percebe* como anterior. Consequência aplicada: o "← Voltar" avulso das páginas de cemitério **saiu** (a seta do menu = histórico, a migalha = hierarquia, os dois já cobrem; `VoltarPagina.tsx` removido) e a migalha do memorial ganhou o nível da cidade (`Início › Cemitérios › Uberlândia — MG › São Pedro › Memorial`), igual à trilha da página do cemitério — antes era uma versão encurtada. **Conferido contra o wireframe do Pedro (tela "Navegação e casca") e corrigido o que divergia:** o cabeçalho **não gruda ao rolar** (`.site-navbar` era `sticky`, virou `relative`); no celular a trilha é `Início › … › penúltimo › atual` e o "…" é um botão que **abre a trilha completa** (antes o penúltimo sumia e o "…" não fazia nada; `Migalhas.tsx` virou client component); `/parceiros/[slug]` ganhou a trilha `Início › nome do parceiro` (era a única página aberta sem). Fontes: [NN/g — Breadcrumbs](https://www.nngroup.com/articles/breadcrumbs/), [Baymard — Back button](https://baymard.com/blog/back-button-expectations), [Smashing — Back button UX](https://www.smashingmagazine.com/2022/08/back-button-ux-design/).

- [x] **Barramento do site público: route group `(site)` com casca única + migalhas** (2026-09-23) — implementa o que o wireframe do Pedro especifica na tela "Navegação e casca": *nenhuma página pública monta o próprio cabeçalho*. As 8 páginas públicas (home, busca, cemitérios ×3, parceiro, privacidade, termos) foram pra `app/(site)/`; o grupo entre parênteses **não entra na URL** (`/busca` continua `/busca`, conferido no build). `app/(site)/layout.tsx` monta `SiteNav`+`SiteFooter` **uma vez** — antes as 8 páginas montavam cada uma por conta própria, e era essa repetição que fazia elas divergirem. `components/public/Migalhas.tsx` (novo): trilha derivada do **dado**, não da URL (quem chega pelo QR, pela busca ou por link vê a mesma); nível que não existe some (nunca um "—"); último nível não é link; home não tem trilha; no celular colapsa em `Início › … › atual` com alvo de 44px; sai também como `BreadcrumbList` em JSON-LD. **Fora do grupo de propósito:** `app/homenagem/[slug]` (regra 21) e `app/fio-da-vida` (casca própria preservada). Referência real: Route Groups do Next.js + [gcascio/next-breadcrumbs](https://github.com/gcascio/next-breadcrumbs), [agadzik/parallel-routes-breadcrumbs](https://github.com/agadzik/parallel-routes-breadcrumbs). **Pegadinha registrada:** `git mv` no Git Bash falha com *Permission denied* em pasta com subpasta dinâmica (`[cidade]`) — resolver com `Move-Item` do PowerShell. **Falta:** ligar `<Migalhas>` em cada página (cada uma declara a própria trilha).

- [x] **Padronização visual do site público: tokens únicos** (2026-09-23) — existiam **3 sistemas de estilo concorrentes**: `lib/publicTheme.ts` (estilo inline, Georgia, 960px, 9 páginas), `site-chrome.css` (menu/rodapé, 1180px) e `app/landing.css` (Inter+Playfair, 1240px). Saía da home e a fonte mudava, a página encolhia e o menu não alinhava com nada. `app/tokens.css` (novo) virou a fonte única da verdade — cor, fonte, escala de título, largura, raio — e a landing passou a consumir em vez de manter cópia. Menu e rodapé saíram de 1180px pra largura oficial: **conteúdo, menu e rodapé finalmente alinham na mesma coluna**. **Privacidade e Termos eram páginas órfãs** (sem `SiteNav`, sem `SiteFooter`, Georgia, 720px, estilo escrito à mão dentro do arquivo — violavam as regras 11 e 12): reescritas com a casca do site e `app/(site)/documento.css`. **3 erros de fato corrigidos na Política de Privacidade**, que é documento legal: citava **Resend** como processadora de e-mail (o envio é Google Workspace SMTP desde 24/07); dizia que a privacidade era "público ou com senha" (são 5 modos); e descrevia contagem de visita acumulada (hoje é 1 por visitante/dia). Plano completo das etapas restantes em `docs/PADRONIZACAO_SITE_PUBLICO.md`. **Falta padronizar:** busca, parceiro e memorial (esse último por último, regras 17/20/21).

- [x] **Páginas de cemitério reconstruídas do protótipo** (2026-09-23, protótipo aprovado pelo Rafael em `wireframe/reestruturacao-site-publico/prototipo-cemiterio.html`) — as 3 páginas (`/cemiterios`, `/cemiterios/[cidade]`, `/cemiterios/[cidade]/[cemiterio]`) saíram do `publicTheme` e entraram no padrão via `app/(site)/cemiterios/cemiterios.css`. Na página do cemitério, o hero centralizado virou o topo do protótipo (dados à esquerda, **"Caminho até o cemitério"** à direita, usando a coordenada real) e **a lista de quem está enterrado ali passou a aparecer ao lado do mapa** — antes só existia como pino, não dava pra ler os nomes; memorial protegido aparece com cadeado e sem foto. **`MapaPublicoCemiterio.tsx` não foi tocado.** O `noindex` da página foi **mantido de propósito** — o wireframe propõe tirar, mas isso expõe nome de falecido no Google, é decisão do Rafael. **Não entrou, por determinação dele (mudança de banco só na etapa de integração):** o bloco institucional do protótipo — logo do cemitério, telefone, WhatsApp, horário de visitação, texto de apresentação, fotos, serviços e planos. **Nenhuma dessas colunas existe em `cemiterios` hoje.** Backup das 3 páginas originais + tema antigo + LEIA-ME em `Desktop\Paginas Originais - Cemiterios\`.

- [ ] **Mapa da landing ainda NÃO está ligado no banco** (pedido do Rafael 2026-09-22, pendente) — a seção "O legado tem um lugar" da home mostra um card **decorativo** (`.mapcard`: retângulo cinza com 1 pino estático em CSS). Ele quer o **Cemitério São Pedro de verdade** (`slug = cemiterio-sao-pedro`, já `publico = true`, 1 memorial real vinculado: Carlos Saraiva), com **pino de cruz** (mesmo padrão do `MapaPublicoCemiterio.tsx`) e **card com o nome do homenageado ao passar o mouse**. Ordem que ele definiu: primeiro a página do cemitério (feita), **depois** ligar a landing e encaminhar o clique pra ela. Detalhe em `docs/RASCUNHO_IDEIAS.md`. **Situação hoje: mapa real funciona na página do cemitério (ortomosaico de drone + pinos do banco); na landing é enfeite.**

- [x] **Landing nova na home + "O Fio da Vida" preservada em `/fio-da-vida`** (2026-09-22, protótipo aprovado pelo Rafael a partir do wireframe do Pedro) — a home deixou de ser peça de venda B2B e virou a porta do público final. Ordem real da página: hero com **busca de memorial de verdade** (`BuscaMemorial`, a mesma da `/busca`, completa enquanto digita) ao lado de um **card de memorial de exemplo** com o mesmo layout da ficha do topo da página do memorial (anel dourado + monograma, "Em memória de", nome em serif, cidade, frase) — fixo num registro fictício (`helena-martins-costa`), **nunca o memorial real de uma família como vitrine**; seção de cemitérios mapeados; "O que a sua família recebe" com os **3 nodes redondos** da landing antiga (mesmas fotos de `/fio-da-vida/nodes/`, acendem no hover, clique abre a ilustração em tela cheia — o fio dourado ligando os nodes **não** veio junto, decisão explícita); os 4 diferenciais que falam com a família; **dois caminhos pra ter o memorial** (parceiro sem custo · direto pelo site em 3 passos, sem pagamento no site); bloco curto de funerária com um mock do dashboard do parceiro; FAQ só da família; citação; rodapé. **Nada foi apagado:** `app/page.tsx` antigo virou `app/fio-da-vida/page.tsx` (rota no ar, intacta, o Rafael vai reaproveitar como peça de apresentação pra família/parceiro ou vídeo), e existe backup completo fora do git em `Desktop\Landing Page Original - Fio da Vida\` (código + dependências + 26 imagens + MD com briefing/estrutura/como replicar, conferido byte a byte). **Menu e rodapé do site aberto reescritos no mesmo commit** (`components/public/SiteNav.tsx`/`SiteFooter.tsx`, compartilhados — mudam de uma vez nas 6 páginas públicas que já os usavam): menu vira Buscar memorial · Cemitérios · Como funciona + botão "Quero um memorial" + "Entrar" (Portal do Parceiro/Família); saíram "Benefícios", "FAQ" (viraram seções, não destinos), o dropdown "Contato" e "Para parceiros" — **a página pública `/parceiros` nunca foi construída**, item de menu apontando pra ela seria link morto. As ações da direita ficam coladas nos links (`.nav-direita`), não grudadas na borda — com 3 itens de menu, `margin-left:auto` abria um vão no meio do cabeçalho. Rodapé passou a ter 4 colunas por público. Parceiro fictício **"Funerária Vale da Paz (exemplo)"** criado em `parceiros_b2b` (marcado nas observações como só-mockup) pro card de dashboard da seção B2B. Protótipo-fonte guardado em `wireframe/reestruturacao-site-publico/prototipo-home.html`. **Pendente dessa virada:** propagar o resto do padrão visual (fonte/cor/espaçamento do protótipo) pras demais páginas públicas — hoje só o menu e o rodapé estão padronizados.

- [x] **Fix: QR Code quebrado em Central e Portal do Parceiro** (2026-08-27) — achado ao verificar pedido do Rafael. Balde `memoriais` virou privado em 18/08 (correção de segurança de mídia), mas `qr_code_url` continuava sendo exibido/baixado com a URL pública antiga (`getPublicUrl`), que passou a responder 400. As 5 telas que mostram QR Code (dashboard Central, ficha Central, dashboard Parceiro, lista Parceiro, ficha Parceiro) tinham `<img>`/`<a download>` direto na URL crua, sem `urlMidiaProtegida()` — mesmo bug já corrigido pro logo do parceiro e pro ortomosaico em 26/08, nunca fechado pro QR. `/api/midia/[...caminho]` já servia `qrcodes/*` sem gate, só faltava as telas usarem o portão — corrigido nos 8 pontos de exibição/download, sem mexer em como o QR é gerado/salvo (banco continua guardando a URL pública, exibição converte na hora, mesmo padrão do logo). E-mail pro fornecedor nunca foi afetado (anexa PNG gerado na hora, não depende da URL salva).
- [x] **Perfil promovida a página oficial do memorial + castiçal de bronze migrado (2026-08-26/27)** — sessão longa de iteração visual (livro de assinaturas, capa, régua lateral) terminou com o Rafael aprovando o resultado e ordenando a promoção: "essa aí vai ser a principal... essa será a nova página oficial do memorial... já pode ligar no Supabase e usar ela como o modelo". `app/homenagem/[slug]/page.tsx` antigo (hero centralizado simples, lista de cartões) movido pra `app/homenagem/[slug]/classico/page.tsx` — preservado intocado, nunca apagado, tag `pagina-classica-antes-da-virada-2026-08-27`. `app/homenagem/[slug]/perfil/page.tsx` (o design novo) movido pra `app/homenagem/[slug]/page.tsx`, virando o modelo oficial. Nenhum QR Code precisou ser regerado (já apontavam pra `/homenagem/[slug]`, que passou a servir o design novo automaticamente). Confirmado antes de promover: o seletor de tema (3 bolinhas) funciona no design novo — testado clicando de verdade em produção — mas **continua quebrado na página clássica preservada**, achado real desta sessão (o container redeclarava as variáveis CSS do tema inline nele mesmo, tapando o que `SeletorTema.tsx` escreve na raiz do documento); corrigido só no design promovido, decisão consciente de não mexer na página congelada. Detalhe completo dos dois modelos em `docs/MODELOS.md`.

  **Junto na mesma sessão**, migração do protótipo de velas que o Rafael desenhou separado (`Desktop\Cerebro Claude - Legado Digital\prototipo-parede-velas\`): mural de 35 velas votivas em canvas com fundo fotográfico (era grade CSS de 45 quadrados sem fundo) e vela principal virou castiçal de bronze com cera escorrendo (era CSS puro) — ambos usando a mesma sprite de chama real da landing "O Fio da Vida", com o mesmo tratamento de alfa. Geometria compartilhada em `lib/muralVelas.ts`/`lib/velaPrincipalCastical.ts`/`lib/chamaAlfa.ts` entre o desenho e o cálculo de onde a chama que "voa" nasce/pousa, pra nunca divergirem. Componente `AcenderVela.tsx` é compartilhado entre a página oficial nova e a clássica preservada — a mudança de vela aparece nas duas. Achado ao migrar: a descrição da regra 20 estava desatualizada em três pontos (dizia "vídeo real" quando a técnica sempre foi CSS puro; "45 velas" virou 35; "anti-spam via `localStorage`" nunca existiu no código), corrigido junto.

- [x] **Mural de velas votivas migrado do protótipo (2026-08-26)** — o Rafael desenhou o novo painel num protótipo isolado (`Desktop\Cerebro Claude - Legado Digital\prototipo-parede-velas\`, HTML único de teste + briefing `MIGRAR-PARA-O-PROJETO.md`) e pediu pra migrar só a parede, sem tocar na vela principal (regra 20). Grade CSS de 45 quadrados cinza idênticos virou mural em `<canvas>` (`components/public/MuralVelasVotivas.tsx`) com fundo fotográfico real (altar: madeira + veludo vermelho + flores, `public/parede-velas-fundo.webp` — convertido de PNG 3,6MB pra WebP 310KB, mesma proporção 2048×1008 exigida pelo banner) e vidro que acende por dentro, não só a chama. **35 velas (5×7), não 45** — as 2 colunas das pontas caíam em cima das flores do fundo escolhido. Chama real: mesma sprite da landing "O Fio da Vida" (`public/fio-da-vida/chama-sprite.png`, hash idêntico confirmado, reaproveitada sem duplicar arquivo), com o mesmo tratamento de alfa (sprite é RGB sem canal alfa — alfa = brilho do pixel, extraído uma vez no carregamento). Ordem de acendimento embaralhada pela parede (seed fixa `20260825`, mesmo algoritmo do protótipo) em vez de encher da esquerda pra direita como barra de progresso — geometria pura extraída pra `lib/muralVelas.ts`, compartilhada entre o desenho no canvas e o cálculo de pouso da chama que "voa" da vela principal (`obterPosicaoDoSlot`, exposto via `useImperativeHandle`), pra nunca divergirem. Loop de reset ao completar a volta mantido do padrão de produção (mais barato que o do protótipo — não desperdiça um clique real, que custa uma escrita no banco, só resetando visual). **Vela principal intocada byte a byte** (`git diff` confirma zero mudança nela, no botão, no contador e na chama que voa) — decisão sobre trocar pra versão em castiçal do protótipo fica pro Rafael, não presumida. **Achado ao migrar, corrigido junto:** a descrição da regra 20 do `CLAUDE.md` estava desatualizada em três pontos (dizia "vídeo real" quando a técnica sempre foi CSS puro; "45 velas" virou 35; "anti-spam via `localStorage`" nunca existiu no código — decisão real é sem limite de cliques, 2026-07-24).
- [x] **Busca embutida direto na landing** (2026-07-30) — botão "Buscar um Memorial" que levava pra `/busca` virou `BuscaMemorial` (mesmo componente da página de busca) embutido direto no hero da landing.
- [x] **Modos de privacidade completos** (2026-07-31, planejado com Opus + executado no mesmo dia, incluindo QR assinado e UI da família na mesma sessão) — os 5 modos existem de verdade: aberto, com senha (já existia), com identificação (cadastro), lista de e-mails autorizados (email, allowlist + OTP de 6 dígitos por 10min), e oculto (some de busca/link/QR pra todo mundo, staff/parceiro/família continuam vendo pelos próprios painéis). Achado no meio do planejamento: já existia um scaffold de schema pra isso desde 2026-07-17 (coluna `modo_gate` + tabelas `memorial_visitantes`/`homenagens_emails_autorizados`/`memorial_email_codigos`) aplicado via MCP mas nunca salvo em `supabase/migrations/` nem usado em código — recuperado retroativamente (`20260717190749_...sql`). Arquitetura: 2 eixos ortogonais — canal (busca/link/QR) × portão (`modo_gate`), resolvidos em ordem fixa por `lib/modosPrivacidade.ts` (`resolverAcesso`): oculto vence tudo → canal errado responde igual a "não encontrado" (nunca "acesso restrito", anti-enumeração) → portão aberto passa → senha/cadastro/email pedem prova. Cookie de acesso ganhou `modo` + `gate_versao` no payload — trocar o modo ou salvar privacidade deriva `gate_versao+1` e derruba na hora qualquer cookie emitido antes, mesmo dentro da validade (30 dias pra senha, 7 pra cadastro/email). UI: componente único `components/admin/PrivacidadeMemorial.tsx` reaproveitado por Central e Portal do Parceiro (radio dos 5 modos + os 3 toggles de canal + gerenciador de e-mails autorizados quando modo=email + lista de visitantes identificados quando modo=cadastro), zero duplicação entre os dois lados. Rotas novas: `/api/memorial-acesso-cadastro`, `/api/memorial-acesso-email/solicitar`+`/confirmar`, `/api/memorial-emails-autorizados` (CRUD da lista), `/api/memorial-visitantes` (leitura) — as 2 de e-mail caem automaticamente no rate limit de login (3/min) do `proxy.ts` por prefixo de path (`/api/memorial-acesso*`), sem precisar mexer no proxy. OTP com rate limit no banco (não no Map em memória do `proxy.ts`, que não é compartilhado entre instâncias serverless da Vercel): 10/hora por memorial, 3/hora por e-mail, 5 tentativas erradas invalida o código, código expira em 10min, resposta sempre genérica — mesmo padrão anti-enumeração de `/api/familia-esqueci-senha`. **QR Code assinado (Fase 7)**: `?qr=<token>` HMAC determinístico (`lib/acessoMemorialSessao.ts`, `criarTokenQr`/`verificarTokenQr`) embutido no QR gerado (`/api/memorial-qrcode` e `lib/dispararEmailFornecedor.ts`, os 2 pontos que geram o PNG) — agora `qrcode_habilitado` e `link_habilitado` são canais de verdade, distintos. Compatível com QR já gerado antes (sem `?qr=` cai em canal "link", nenhuma placa existente quebra). **UI da família (`/familia/[slug]`)**: card "Privacidade" com o mesmo editor completo de Central/Parceiro (radio dos 5 modos + 3 toggles de canal + allowlist/visitantes) — confirmado pelo Rafael no mesmo dia que a família pode travar/destravar o próprio memorial sozinha, sem precisar pedir pra funerária. `/api/memorial-privacidade` passou a aceitar o tripé staff/parceiro-dono/família (`lib/autorizacaoMemorial.ts`, mesmo helper de `/api/memorial-emails-autorizados`) em vez de só staff/parceiro. `/api/familia-memorial` (GET) devolve também um bloco `privacidade` (lido de `homenagens_seguranca`, já que a família não tem RLS direta nessa tabela) usado como estado inicial do editor.
- [x] **Imagem de capa do memorial escolhida pela família** (2026-09-09, pedido do Pedro na reunião) — topo da página do memorial ganhou banner opcional atrás do retrato/nome, com véu escuro terminando na cor do tema (texto nunca perde contraste). **Catálogo fechado de 6 paisagens** (`lib/bannersMemorial.ts`, arquivos em `public/banners/*.webp`, geradas em RealVisXL 1536×640, sem pessoa e sem texto): montanha, mar, floresta, trigo, céu estrelado, jardim. A família **escolhe**, não sobe imagem — decisão consciente: arquivo de família varia proporção/enquadramento/peso e foto retrato de celular esticada em faixa 24:10 quebraria o topo; catálogo elimina o risco de imagem estourada por construção. Coluna `homenagens.banner_capa` guarda o **id** do banner (nunca a URL — arquivo pode trocar de nome/formato sem virar link morto), nula por padrão, acrescentada ao fim de `homenagens_publica` com `security_invoker` reaplicado. Seletor fica no Portal da Família junto do tema/laterais; `/api/familia-memorial` valida contra o catálogo no servidor (POST montado à mão não grava string livre) e `resolverBanner()` faz id desconhecido cair em "sem capa" em vez de imagem 404. PNGs de ~1MB convertidos pra WebP (50-250KB + miniatura de 3-17KB pro seletor).
- [x] **Decoração das laterais desligada por padrão + capa e laterais viram escolha exclusiva** (2026-09-09, achado ao vivo com o Rafael) — com a imagem de capa ligada, as faixas laterais (`AmbienteLateral`, pontos de luz/pétalas em canvas, `position:fixed` cobrindo a largura toda) **cortavam a capa nas bordas da tela** — duas decorações disputando a mesma borda. Decisão do Rafael: "aquilo não rolou, pode tirar, deixa desligado". `homenagens.ambiente_lateral` passou a ter default `'nenhum'` (era `'pontos'`) e as 7 linhas existentes foram atualizadas; todo fallback de código que assumia `'pontos'` virou `'nenhum'` (página pública, Portal da Família, prop default do componente). A decoração **não foi removida** — continua inteira, agora atrás de uma **chave de liga/desliga** no Portal da Família (antes só existia como o botão "Nenhum" no meio das opções, que ninguém lê como "desligar"); com a chave desligada, as opções de ambiente e a cor das laterais somem da tela. E os dois passaram a ser **mutuamente exclusivos**: escolher uma capa desliga a decoração, ligar a decoração tira a capa, com a explicação escrita nas duas telas (regra 10) — a família decide qual das duas quer, em vez de descobrir o corte depois de publicar.
- [x] **Motivo do erro visível direto no sino** (2026-09-10, pedido do Rafael) — dropdown do sino já tinha bolinha verde (sucesso) / vermelha (erro) por item, clicável pro memorial; faltava mostrar POR QUE deu erro sem precisar abrir `/admin/emails`. `erro_msg` (coluna que já existia, já usada na tela cheia de Comunicações) entrou no select do header e aparece em vermelho embaixo do item quando `status='erro'`.
- [x] **Quem cadastrou o memorial, vinculado por login** (2026-09-10, pedido do Rafael) — nova coluna `homenagens.criado_por_usuario_id` (FK pra `usuarios.id`, `on delete set null`), gravada sozinha nos 3 pontos onde memorial nasce (Central, Portal do Parceiro, cadastro direto no mapa do cemitério) via helper novo `obterUsuarioIdAtual()` (`lib/auth.ts`) — nunca um campo de texto digitado, elimina erro/esquecimento/mentira. Aparece como chip só-leitura na ficha dos dois portais e como coluna na lista de ambos. **Protótipo aprovado antes de codar** (`opendesign/mockups/cadastrado-por/prototipo.html`), a pedido do Rafael. Memoriais de antes da mudança ficam sem essa informação — não dá pra descobrir retroativamente; único caso atribuído manualmente foi o Carlos Saraiva → Pedro (confirmado pelo Rafael).
- [x] **Botão "ver senha" em todo campo de senha do sistema** (2026-09-11, pedido do Rafael) — os 8 campos de senha do sistema (login Central, login Parceiro, redefinir senha ×2, trocar senha obrigatória ×2, busca pública com senha, gate de senha do memorial) ganharam ícone de olho (`lucide-react` Eye/EyeOff) que alterna `type="password"`/`type="text"`, mantendo o estilo visual de cada tela (zinc do admin, dourado/navy do público). Nenhum campo ficou de fora — checado com `grep -rn 'type="password"'` no projeto inteiro antes de fechar.
- [x] **Login com Google na Central + fundação da integração Google Workspace** (2026-09-11, pedido do Rafael) — botão "Entrar com Google" em `/admin/login`, restrito a contas `@legadodigital.net` (tela de consentimento OAuth "Interno" no Google Cloud, que já barra qualquer conta fora do domínio antes de chegar no sistema — segurança por construção, não checagem de código). No mesmo clique, pede também acesso a Gmail/Calendar/Drive **da própria pessoa** (`gmail.readonly`, `gmail.send`, `calendar.events`, `drive.readonly`) — decisão explícita do Rafael: "não quero ver a caixa do meu sócio", cada staff só acessa o próprio, nunca o de outro, nem ele como admin. Tabela nova `google_tokens` (1 linha por `usuario_id`, RLS só o próprio dono lê/escreve — nem `service_role` bypassa sem querer, é `auth.uid() = usuario_id` puro) guarda o token, já que o Supabase não persiste o token do provedor sozinho (confirmado na doc oficial antes de codar). Rota `/api/admin/google-tokens` grava o token de quem acabou de logar — identidade sempre vem do `Authorization: Bearer`, nunca de um id mandado no corpo. Guarda existente em `app/admin/layout.tsx` (`getAdminUser()`) já barra quem loga pelo domínio mas não é staff cadastrado — sem código extra. Login por e-mail+senha continua existindo, nada foi removido. **Infraestrutura Google Cloud criada no mesmo dia** (projeto `legado-digital-508311`, conta de serviço com Delegação em Todo o Domínio autorizada pro `contato@legadodigital.net`, credenciais fora do repositório — ver `docs/USO_SKILLS_MCPS.md`) — ainda faltam 3 das 4 integrações combinadas: caixa de e-mail do `contato@` em Comunicações (usa a conta de serviço, não o login pessoal), agenda na ficha do lead/parceiro, anexar do Drive.
- [x] **Mapa "Como Chegar" vira modal, aberto pelo botão que já existia no topo** (2026-09-14, pedido do Rafael — item da ata de sócios) — o botão "Guia até o túmulo dentro do cemitério" no topo do memorial já existia, mas só rolava até a seção lá embaixo (`#localizacao`), que ainda precisava de um segundo clique (`<details>` retrátil) pra abrir o mapa de fato. Confirmado explicitamente com o Rafael antes de mexer (regra 17): a seção de baixo sai, o botão do topo passa a abrir o mapa direto, em modal de tela cheia. `components/public/GuiaTumuloModal.tsx` (novo) é só um invólucro — botão com o mesmo texto/estilo de antes + modal (`role="dialog"`, Esc fecha, trava scroll do fundo) contendo o `GuiaTumulo`/`GuiaTumuloCarregador` **sem nenhuma alteração** na lógica de mapa/rota. O link "Localização" do menu do topo (que apontava pra `#localizacao`) também passou a abrir o mesmo modal, via interceptação de clique — antes disparava scroll morto pra uma âncora que deixou de existir. `app/homenagem/[slug]/page.tsx`: seção `<section id="localizacao">` antiga (com o `<details>` e o script que forçava a âncora abrir) removida por inteiro; o botão do topo agora é o `GuiaTumuloModal` com exatamente os mesmos props que a chamada antiga usava.
- [x] **Guia do túmulo abre direto em tela cheia + mapa público ganha tela cheia** (2026-09-15, achados ao vivo com o Rafael testando o modal do item acima) — 3 correções em sequência: (1) o mapa interno do `GuiaTumulo` nascia recolhido atrás de botão próprio (fazia sentido lá embaixo, não dentro de um modal já aberto de propósito) — corrigido simulando o clique nesse botão assim que aparece no DOM, sem tocar no `GuiaTumulo.tsx` (regra 17); (2) o mapa tinha altura fixa de 480px, cortado dentro do modal em tela cheia — corrigido via CSS mirando só `.maplibregl-map` escopado ao modal (`calc(100dvh - Npx)`), sem mexer no arquivo protegido nem na página clássica que o reaproveita (regra 21); (3) "Rota de carro" e o botão retrátil duplicavam o que já existe fora do modal — escondidos só visualmente (CSS por posição), componente por dentro intocado. **Mapa público de cemitério** (`MapaPublicoCemiterio.tsx`) ganhou o mesmo botão de tela cheia (Fullscreen API + `position:fixed 100dvh`, funciona em qualquer aparelho incluindo iOS) e altura inline maior (620px, era 480px) — pedido do Rafael: "o campo pra visualizar tá pequeno".
- [x] **Retrato do memorial abre em tela cheia ao clicar** (2026-09-15) — `FotoRetratoTelaCheia.tsx` (novo), mesmo padrão dos mapas (Fullscreen API + fallback `position:fixed`, "← Voltar" explícito). 2 ajustes achados ao vivo: a imagem só tinha `maxWidth/maxHeight` (limitava crescimento mas nunca fazia a foto pequena preencher a tela — trocado pra `width/height:100%` + `objectFit:contain`); e o Chrome mostrava o `aria-label` do botão como balão nativo ao passar o mouse (`title=""` no botão suprime, padrão conhecido pra isso).
- [x] **Capa do memorial vira fundo do topo (atrás do retrato e do bloco de mídia), não mais faixa solta** (2026-09-15, revisão do item de capa de 09/09 — 2 prints anotados do Rafael pedindo ajuste) — a capa tinha virado uma faixa full-bleed acima do hero (09/09), depois contida na largura da página mas ainda como bloco separado (09/14); agora é o próprio fundo do `<header>` (classe `mem-hero-com-capa`), atrás do retrato/nome/galeria, contida no mesmo container do nav (nunca de ponta a ponta). `background-repeat:no-repeat` corrigiu a imagem "dobrando" nas bordas laterais. Véu em gradiente escurece progressivo até a cor sólida do tema.
- [x] **Nav e rodapé do memorial viram o padrão real do site** (2026-09-15, pedido do Rafael: "tudo padrão") — logo do nav ajustada pro mesmo tamanho (86px) usado no `SiteNav.tsx` das outras páginas públicas (estava menor). Rodapé trocado pelo `SiteFooter.tsx` compartilhado (antes era montado à mão, com selos de confiança e 3 links soltos) — mesma logo/colunas/copyright de busca/cemitérios/parceiro. **Achado real:** o rodapé compartilhado nasce com fundo fixo `#081722` (certo nas outras páginas, que não trocam de cor); o memorial troca de paleta ao vivo (`SeletorTema`), então a cor do rodapé ficava presa. Corrigido em `site-chrome.css` trocando o valor fixo por `var(--mem-fundo-profundo, #081722)` — fallback idêntico ao valor antigo, zero mudança nas páginas que não definem essa variável, só o memorial (que já define) passa a acompanhar. (Uma primeira tentativa, regra CSS separada em `globals.css` escopada por `.perfil-page`, simplesmente não apareceu no CSS final de produção mesmo após deploy confirmado — bug de minificador não investigado a fundo, abandonada em favor da abordagem de arquivo único.)
- [x] **Seta do topo do memorial volta pra página anterior de verdade, não sempre pro site** (2026-09-15, pedido do Rafael) — `VoltarLink.tsx` (novo, client component) usa `router.back()` quando há histórico de navegação dentro do próprio site (checado via `document.referrer`); cai no site (`/`) quando a pessoa chegou direto de fora (QR Code, link direto) — sem "página anterior" real pra voltar.
- [x] **Card do jazigo no mapa da Central: só abre com memorial, lista quem está em cada gaveta** (2026-09-15, pedido do Rafael com print anotado) — o card grande do clique no pino virou a ficha do jazigo: **nome do jazigo** no topo (coluna nova `lapides.nome`, ex: "Jazigo Família Saraiva"), depois **cada gaveta com seu ocupante** (`G1`, `G2`… derivado de `gavetas.linha`) — quem tem memorial aparece com foto e vira link pra **página pública** dele (`/homenagem/[slug]`, nunca a ficha de edição), quem não tem fica como texto. **Achado real da sessão:** o nome do Carlos Saraiva **já estava sendo renderizado**, invisível — o link usava `color:'inherit'` e o popup do MapLibre tem fundo branco fixo, então o texto herdava a cor clara do tema escuro da Central e sumia no branco; só aparecia o que tinha cor explícita (código da gaveta em cinza, botões coloridos). Por isso o memorial parecia desvinculado com o banco 100% correto (`gavetas.homenagem_id` e `homenagens.lapide_id` conferidos por SQL, ambos certos). Todo texto do card passa a ter cor explícita. **Card só abre em jazigo que já tem memorial**, por qualquer caminho (clique no pino, botão direito, e o próprio render tem a guarda) — túmulo vazio não abre card nenhum, criar memorial ali continua no botão direito. **Saiu do card** (edição de verdade mora na página do jazigo): criar memorial aqui, adicionar nome sem memorial, trocar foto, cadastrar gavetas. **Ficou** só o que se lê ou se resolve na hora: os ocupantes, anexar/tirar a foto da lápide, desvincular, vincular memorial existente, mover pino, e o atalho "Abrir página do jazigo →". **Card pequeno de hover sai deste mapa** (tela de trabalho, o card de clique já diz tudo) — continua no Portal da Família e no mapa público, onde só o nome basta. `lapides.nome` é texto livre em vez de só o sobrenome porque nem todo jazigo é de família (capela, perpétuo numerado, institucional) — forçar "Família X" geraria rótulo errado; vazio cai no código técnico, nenhum jazigo existente quebra. A **página do jazigo** (`/admin/cemiterios/[id]/lapides/[lapideId]/gavetas`) ganhou as 2 edições que saíram do card: renomear o jazigo e escrever o nome de quem está na gaveta sem memorial digital (sem isso `gavetas.nome_sem_memorial` ficaria sem nenhuma UI).
- [x] **Ficha do parceiro vira abas, mesma estrutura do Portal da Família** (2026-09-16, print anotado do Rafael mostrando espaço vazio e retráteis desorganizados) — `/admin/parceiros/[id]` era um grid solto com `SecaoRetratil`. Virou abas na ordem de prioridade de preenchimento: **Dados** (razão social/CNPJ/e-mail/telefone — achado real anotado na própria tela: nunca teve formulário de edição, só mostra o que já está no banco), **Contatos e Acesso** (lista de contatos + "Conceder acesso avulso" — eram 2 seções retráteis fazendo quase a mesma coisa, viraram uma aba só, nada removido), **Página Pública** (logo, descrição), **Memoriais** (tabela), **Plano** (plano/pagamento/desde). Cabeçalho (nome, tipo+cidade, badge Ativo/Inativo, "Acessar Plataforma do Parceiro") fica fixo acima das abas. Wireframe com 5 opções em `wireframe/1609-cadastro-parceiro/`, Opção 1 aprovada e implementada.
- [x] **Ficha do memorial única pra Central e Portal do Parceiro, em abas** (2026-09-16, pedido do Rafael: "essa página aí tem que ser a nossa também") — `components/admin/FichaMemorial.tsx` (prop `modo: central|parceiro`); `app/admin/memoriais/[id]` e `app/parceiro/memoriais/[id]` viraram só invólucro. Ficha antiga da Central (coluna longa com `SecaoRetratil`) saiu. Abas na ordem pedida: **Cadastro Família · Memorial · QR Code e Placa · Fotos e vídeos · Privacidade · Moderação**, abrindo em Cadastro Família; "Quem preenche o conteúdo" fica no topo da aba Memorial. Diferença por modo: parceiro só abre memorial do próprio `parceiro_id` (regra 22); Central mostra "Vinculado a [parceiro]" e auto-gera QR faltante (só com nome e data de falecimento reais). "+ Novo Memorial" da Central deixou de abrir a janela curta de cadastro: cria o rascunho na hora (sem parceiro, `origem_cadastro=central`) e abre direto a ficha em abas, igual ao parceiro; rascunho da Central que nunca ganhou nome some depois de 2h. Os dois lados ganharam o aviso de presença (outra pessoa editando) e o botão "Acessar Portal da Família" — `/api/admin/acessar-familia` e `useTravaEdicao` já aceitavam parceiro dono.
- [x] **Busca no mapa do cemitério** (2026-09-16, pedido do Rafael) — campo "Encontrar no mapa" acima do mapa (`MapaCemiterio.tsx`, vale pra Central e pro modo leitura do Portal do Parceiro): completa enquanto digita e acha memorial (nome), jazigo (nome ou código), quadra, fileira ("Quadra N · Fileira M") e rua. Filtra em memória o que o mapa já carregou (sem consulta nova, então parceiro só acha o que a RLS já deixou ele ver). Memorial/jazigo: voa até o túmulo, abre o card se tiver memorial e marca o pino com anel dourado; quadra/fileira/rua: enquadra a área.
- [x] **Página do jazigo em abas** (2026-09-16, pedido do Rafael) — `/admin/cemiterios/[id]/lapides/[lapideId]/gavetas`: nome e código fixos no topo, abas **Cadastro do Jazigo** (nome editável direto) · **Foto da Lápide** · **Gavetas** (formulário e lista lado a lado) · **Gavetas 3D** (a visualização 3D entra como aba, montada só quando aberta). Rota antiga `/gavetas-3d` continua existindo.
- [x] **Atalhos de túmulo e memorial corrigidos** (2026-09-16, regra fixa do Rafael) — na tela de Jazigos (quadradinho, "Túmulo N", resultado da busca por código) e no painel de quadras do mapa: túmulo abre a **página do jazigo**; nome do memorial abre a **página pública** (`/homenagem/[slug]`), não a ficha de cadastro. O popup antigo do quadradinho saiu.
- [x] **Tela de Jazigos em 3 níveis** (2026-09-16, wireframe `wireframe/1609-jazigos-quadras/simples.html` aprovado pelo Rafael) — `/admin/cemiterios/[id]/lapides` deixou de abrir todas as quadras lado a lado com quadradinhos numerados: (1) grade de quadras + "Ir pra quadra" + cartão "Fora de fileira"; (2) dentro da quadra, abas por fileira; (3) quadrados de jazigo — nome do jazigo em cima (sem nome: "Túmulo N") e embaixo quem está lá, lido das **gavetas**; quem tem memorial aparece com foto e nome dourado (link pra página pública), quem não tem fica só o nome. Quadrado abre a página do jazigo. Carrega só a fileira aberta; quadra/fileira no endereço (`?quadra=36&fileira=1`). Busca única por memorial, nome do jazigo ou código. Legenda antiga dos quadradinhos e lista solta "Túmulo N — nome" saíram.
- [x] **Gavetas 3D refeito como jazigo subterrâneo em corte** (2026-09-17, wireframe `wireframe/1709-gavetas-jazigo/modelo-corte.html` aprovado a partir de imagem de referência do Rafael) — `JazigoGavetas3D.tsx`: chão de grama com abertura no meio, câmara de concreto aberta na frente, corredor central, gavetas sempre divididas meio a meio entre os dois lados do corredor, automaticamente, na ordem do cadastro, de cima pra baixo (4 gavetas = 2 de cada lado; não depende da coluna cadastrada), **sem tampa removível** (pedido explícito). Cor por gaveta: dourado = memorial, cinza escuro = nome sem memorial, cinza claro = vaga; nome em etiqueta ao lado de cada gaveta. Botões girar, zoom, vista de frente/ângulo. Painel ao lado com foto, quem está, andar, lado, "Abrir página do memorial", "Editar gaveta" (leva pra aba Gavetas já editando) e a ocupação. Numeração/andar/coluna ajustáveis depois na aba Gavetas — o desenho se refaz sozinho.
- [x] **Dashboard da Central enxuto** (2026-09-17, Opção 2 de `wireframe/1709-dashboard` aprovada pelo Rafael) — `/admin` virou só 6 números (visitas, novos memoriais 7d, homenagens 7d, Parceiros/Memoriais/Usuários clicáveis) + rankings de cemitérios e parceiros com mais visita. "Memoriais com dados faltando" e "E-mail do fornecedor de placas" foram pra Central de Comunicações (`PendenciasMemoriais.tsx`, `EmailFornecedorPlacas.tsx`); a tabela de QR Codes virou coluna "QR Code → Baixar" na lista `/admin/memoriais`.
- [x] **Visitas reais nos memoriais + todos os cemitérios no Dashboard** (2026-09-17, pedido do Rafael: "números têm que ser verdadeiros") — o contador somava +1 a cada carregamento da página (recarregar, robô, prévia de link, automação): Carlos Saraiva estava com 27.648 em 31 dias. Agora `lib/registrarVisita.ts` conta **1 visita por visitante, por memorial, por dia** (tabela `memorial_visitas`, visitante = HMAC de IP+navegador+dia, nada identificável guardado; RPC `registrar_visita` só service_role), ignorando robô/prévia de link/prefetch. `incrementar_visualizacao` aposentada (execute revogado). **Contador antigo zerado** a pedido do Rafael, com backup antes (`legado-digital-2026-09-17T12-04-05.sql`). `admin_dashboard_metricas` passou a listar **todo cemitério e todo parceiro** (mesmo com 0, com link) e a não contar rascunho como memorial novo; total de Memoriais do Dashboard também ignora rascunho. Achado junto: os registros mostravam o Dashboard sendo recarregado a cada ~2s no navegador do Rafael (automação "OpenClaw" depurando o Chrome) — provável origem da inflação.
- [ ] Website institucional finalizado
- [x] Contatos da empresa (`parceiros_contatos`) ligados ao fluxo de convite existente — já funciona: contato cadastrado com e-mail dispara convite automático, contato sem e-mail tem botão "Conceder acesso" próprio, `POST /api/admin/convidar-parceiro` grava `usuario_id` de volta na linha do contato via `contatoId`. Item estava desatualizado na lista, corrigido 2026-07-30 (regra 4).
- [x] **Privacidade em 3 toggles levada pro `/parceiro/memoriais`** (2026-07-30) — antes só existia em `/admin/memoriais/[id]`; API `/api/memorial-privacidade` já aceitava parceiro dono (auditoria anterior), faltava só a UI/estado no lado do Parceiro — copiado igual ao de Central (busca/link/QR, todos ligados por padrão).
- [x] "Esqueci a senha" da família — self-service já existe (botão na tela `/familia/login` → `/api/familia-esqueci-senha`, resposta genérica anti-enumeração, senha nova por e-mail). Item estava desatualizado na lista, corrigido 2026-07-30 (regra 4).
- [x] **Suporte a múltiplos vídeos por memorial** (2026-07-31) — estava pausado, retomado a pedido do Rafael. Nova coluna `videos_galeria text[]` (CHECK máx 4, mesmo padrão de `galeria_fotos`) — `video_url` continua sendo o vídeo "capa" (aceita YouTube), `videos_galeria` são extras, só upload direto. Refletido nos 4 lados: Central e Portal do Parceiro (upload múltiplo + grid com remover, mesmo padrão da galeria de fotos), Portal da Família (`/api/familia-upload` ganhou a pasta `videos_galeria`, exclusa da auto-limpeza de arquivo único que só vale pra `foto`/`video`), e página pública (seção "Mais vídeos" depois do vídeo principal). `homenagens_publica` ganhou a coluna (sempre no final da lista de colunas — `create or replace view` não deixa reordenar/renomear coluna existente) e `security_invoker=on` foi reaplicado na mesma migration (senão cai de novo, mesmo achado desta sessão).
- [x] **Troca de tema persistente** (2026-07-31) — nova coluna `tema text default 'navy'` (`navy`/`verde`/`grafite`, mesmas 3 paletas de `lib/temasMemorial.ts`). Separação de responsabilidade: quem escolhe o tema de verdade é Central/Parceiro/Família no formulário de edição (salvo junto dos outros campos, mesmo padrão de `vinculos`) — o `SeletorTema.tsx` na página pública continua sendo só preview ao vivo pro visitante (nunca salvava, e não devia: se salvasse ali, qualquer visitante mudaria a cor pra todo mundo), só passou a abrir já no tema real (`temaInicial`) em vez de sempre `navy`. A página pública aplica o tema no primeiro render (SSR) via custom properties CSS inline no container principal — zero flash de cor errada, sem JS. `homenagens_publica` ganhou a coluna (final da lista + `security_invoker=on` reaplicado, mesmo padrão desta sessão).
- [x] **Limpeza dos avisos WARN do scanner de segurança** (2026-07-31, `get_advisors`) — 6 achados fechados, nenhum era vulnerabilidade ativa (verificado um por um antes de mexer): `admin_dashboard_metricas` ganhou `search_path` fixo (mesmo padrão das outras funções); `atualizar_pagina_publica_parceiro` já se protegia por dentro (checa `auth.uid()`, sempre falha pra `anon`) mas o `EXECUTE` pra `anon` foi revogado mesmo assim (só `authenticated` chama de verdade); `incrementar_visualizacao`/`obter_localizacao_memorial`/`set_updated_at` perderam `EXECUTE` de `anon`/`authenticated` — a página pública lê tudo com service role desde a correção de segurança desta sessão, nenhuma dessas RPCs tem chamador legítimo fora do service role mais. **Mesma pegadinha do `acender_vela` apareceu de novo:** revogar só de `anon`/`authenticated` não bastava, `PUBLIC` (grant implícito de quando a função foi criada) ainda liberava pra todo mundo — teve que revogar de `PUBLIC` explicitamente e testado como `anon` real depois (as 4 bloqueadas). **Deixados de propósito, não são furo:** `is_legado_staff()`/`is_own_parceiro()` executável por `authenticated` — preciso pra RLS funcionar (toda policy que usa essas funções depende do `authenticated` ter `EXECUTE` nelas, revogar quebraria toda a hierarquia de acesso); `homenagens_busca_publica` sem `security_invoker` — decisão antiga já documentada (exporia hash de senha); extensão `unaccent` no schema `public` — mover exigiria atualizar `search_path` de toda função que usa (`buscar_homenagens_publicas` e possivelmente outras), risco de quebrar a busca por pouco ganho, não fiz sob pressão de tempo. **Pendente, precisa ação manual do Rafael (não dá via SQL/MCP):** "Leaked Password Protection" do Supabase Auth desligada — é config do painel (Authentication → Providers → Email), não uma tabela — liga em 1 clique, sem custo, bloqueia staff/parceiro de cadastrar senha já vazada/conhecida (checagem contra HaveIBeenPwned).
- [x] **Rate-limit de escrita pública** (2026-07-30) — condolências, mural (criar + reagir) e vela agora passam por Route Handlers próprios (`/api/memorial-condolencia`, `/api/memorial-mural`, `/api/memorial-mural-reagir`, `/api/memorial-vela`), com service role, cobertos pelo rate limit geral do `proxy.ts` (30/min por IP em `/api/*`) mais um limite por-recurso (`lib/rateLimitUtil.ts`: por memorial e por IP, ex: 50 condolências/hora por memorial). O bypass direto foi de fato fechado, não só movido: migration `20260730_revoke_public_writes_condolencias_mural_vela.sql` removeu a policy de INSERT público em `condolencias`/`mural_memorias` e revogou `EXECUTE` de `anon`/`authenticated`/`PUBLIC` nas RPCs `reagir_memoria`/`acender_vela` — testado como role `anon` direto no banco, insert e RPC bloqueados. `visualização` (contagem de views) roda dentro do próprio SSR de `/homenagem/[slug]`, então em vez de reescrever esse fluxo só adicionei `/homenagem/` à lista de paths cobertos pelo `proxy.ts` (30/min por IP na página inteira, que já inclui a contagem).
- [x] **Auditoria de segurança completa (Opus, 2026-07-24)** — achado grave: `app/middleware.ts` nunca rodou em produção (Next.js 16 renomeou a convenção pra `proxy.ts` na raiz) — zero rate limit em qualquer rota até hoje. Corrigido, mais 5 itens: chat público do LegadoBot sem trava de origem/vazamento de erro, consulta de CNPJ sem autenticação, headers de segurança site-wide (CSP/HSTS/X-Frame-Options), rota de backup com bug que sempre bloqueava por acidente (corrigida com auth de verdade), `sharp` desatualizado (CVEs reais, lib que processa foto de família) atualizado via `overrides`. Detalhe completo no vault.
- [x] **2 vazamentos de dados entre parceiros + 11 achados de lógica/integração (2026-07-27)** — parceiro sem vínculo via bug de RLS via cross-tenant via URL `?parceiro_id=`, `getAdminUser()` sem checar papel, e-mail da família exposto na leitura pública, sem rate limit de senha, mural sem moderação, slug duplicado sem trava (quebrava login da família), upload sem limite no Storage, race condition de edição concorrente (nova coluna `homenagens.updated_at`), barra de armazenamento decorativa, rascunho órfão público, coração do mural sem persistir. Todos corrigidos, buildados, no ar. Detalhe completo nos Dev Logs de 2026-07-27 do vault.
- [x] **UI de moderação do mural de memórias** (2026-07-29) — botão "Remover" na ficha, Central e Portal do Parceiro, usando a permissão de DELETE já liberada em 2026-07-27.
- [x] **Troca de senha obrigatória no 1º login do parceiro** (2026-07-29, planejado com Opus) — senha temporária deixou de ser fixa "123456", agora gerada por parceiro (`lib/gerarSenhaTemporaria.ts`: 2 letras do nome + 6 dígitos aleatórios). Nova coluna `usuarios.senha_temporaria`; `app/parceiro/layout.tsx` bloqueia com tela cheia (`TrocarSenhaObrigatoria.tsx`) até trocar — só pra convite novo, staff em modo Central nunca cai nessa tela.
- [x] **Mapa "Como Chegar" sem imagem de satélite** (2026-07-29, achado crítico ao vivo com o Rafael) — CSP (auditoria 2026-07-24) faltava `server.arcgisonline.com` no `connect-src` e `worker-src` pra blob: (MapLibre usa fetch()+web worker, não `<img>` como o Leaflet antigo que a CSP foi calibrada pra). Corrigido só em `next.config.ts`, sem tocar em `GuiaTumulo.tsx` (regra 17). De quebra: pino do túmulo agora mostra foto+nome ao passar mouse/tocar (pedido explícito), e nota no rodapé do mapa avisando que a imagem de satélite será substituída pelo ortomosaico do drone quando existir.
- [x] **`/parceiro/emails` sem filtro por empresa** (2026-07-29) — staff em modo Central via e-mail de qualquer parceiro ali, corrigido com o mesmo padrão de filtro já usado no resto do Portal do Parceiro.
- [x] **3 achados de segurança baixa prioridade** (2026-07-29) — 13 RLS policies restritas de `public` pra `authenticated`; sessão de família/senha de memorial passou a usar `SESSION_HMAC_SECRET` próprio em vez de reaproveitar `SUPABASE_SERVICE_ROLE_KEY` (já configurado na Vercel + `.env.local`).
- [x] **Auditoria de segurança Central/Parceiro/Família (Opus, 2026-07-29)** — pedido do Rafael pra confirmar que os 3 níveis nunca se cruzam. Achado crítico real: `parceiros_publicos` e `homenagens_busca_publica` (views) rodavam sem `security_invoker` (privilégio do dono, ignora RLS) e tinham GRANT de escrita pra `anon` — qualquer requisição com a chave pública conseguia `DELETE`/`UPDATE` direto num parceiro via REST, sem login nenhum (cascade derrubava login da funerária e orfanava memoriais). Corrigido em 3 migrations: revoke de escrita imediato, depois `security_invoker` real em `parceiros_publicos` (com correção no meio do caminho — a primeira tentativa da policy vazava email/cnpj/telefone pra qualquer `authenticated`, não só `anon`, corrigida antes de virar risco). `homenagens_busca_publica` manteve o padrão atual de propósito (documentado no SQL) — teria que expor a coluna de hash de senha pra funcionar como invoker. Mais 6 achados médios/baixos, todos corrigidos: view `homenagens_publica` com colunas explícitas substituindo GRANT genérico em `homenagens` pra leitura pública (fragilidade, não vazamento ativo); `/api/memorial-storage-usage` sem autenticação nenhuma, agora aceita staff/parceiro dono ou sessão de família; 2 rotas de upload mortas (`admin/upload`, `parceiro/upload`) removidas; login da família passou a exigir e-mail cadastrado junto da senha (senha sozinha era só 32 bits) — **revertido em 2026-08-17 a pedido do Rafael**, ver item de 17/08 abaixo; `/api/familia-memorial` com `select('*')` virou lista de coluna explícita; sessão da família (cookie 12h) agora revoga na hora quando a senha troca (token embute pedaço do hash vigente); parceiro não conseguia salvar a própria página pública (RLS só tinha policy de UPDATE pra staff) — corrigido com função `SECURITY DEFINER` estreita em vez de abrir policy genérica (evitaria vazar `status_pagamento`/`cnpj` pra escrita do próprio parceiro). E-mail/telefone/nome da família confirmados como NUNCA vazados (GRANT por coluna já funcionava, testado com query real como `anon`). Nada do que foi mexido nas fichas hoje (cadastro de família, QR Code, CPF) tinha problema de cruzamento. 9 commits separados, todos com build/typecheck verde antes de subir.
- [x] **QR Code verificado + bug de reenvio pro fornecedor corrigido** (2026-07-29, pedido urgente do Rafael) — a URL embutida no QR sempre vem do slug fresco no banco pelo `memorialId` (`/api/memorial-qrcode`), sem risco de apontar pro memorial errado. Achado real: no Portal do Parceiro, `salvar()` regenerava o QR **e reenviava e-mail pro fornecedor da placa física a cada "Salvar"** (qualquer edição, não só a primeira vez) — sem checagem de já ter sido enviado, risco de pedido duplicado de placa física. Corrigido pra só gerar/enviar no primeiro save (quando `qr_code_url` ainda não existe). Na Central isso nunca foi bug (só gera no diálogo de criação, nunca de novo no `salvar()`). Confirmado também: criação no Portal do Parceiro ("+ Novo Memorial") não gera QR na hora (diferente da Central) — decisão consciente, não corrigida: geraria QR/e-mail pro fornecedor pra um registro ainda vazio ("Novo memorial"), então o gatilho certo é o primeiro save com dado real, não a criação do rascunho.
- [ ] **Módulo de pagamento/cobrança pros parceiros** — plano de arquitetura pronto (Opus, 2026-07-29): começar só com registro estruturado de contrato/consumo (sem gateway ainda, empresa pré-CNPJ não consegue nota fiscal/conta PJ); quando integrar, recomendado Asaas (boleto/PIX/NFS-e, recorrência anual). Aguardando decisão de preço/modelo do Rafael.
- [ ] **Central de Comunicações bidirecional (e-mail+WhatsApp)** — plano de arquitetura pronto (Opus, 2026-07-29): receber e-mail via polling do Gmail API; WhatsApp só via Meta Business Cloud API oficial (não-oficial banido sem aviso, risco alto). Aguardando o Rafael criar conta Google Cloud + Meta Business antes de qualquer código.
- [x] **Mapa do cemitério com ortomosaico real de drone** (2026-08-04, planejado com Opus + primeira captura real integrada no mesmo dia) — a ideia registrada em 2026-07-29 ("Mapa satélite/3D do cemitério inteiro") saiu do papel direto com dado real: o Rafael Rassi (drone) mapeou o **Cemitério Municipal José Lázaro** (Tupaciguara/MG, em frente ao Fórum Adolpho Fidélis dos Santos — cadastrado nesse dia, nenhum cemitério existente ficava perto de onde o voo aconteceu, o mais próximo estava a ~50km). Resolução real: **2,2cm/pixel**, túmulo individual ocupa ~68×34px no zoom máximo — nítido o suficiente pra distinguir cada um, confirmado visualmente (não só no papel).

  **Pipeline** (`scripts/ortomosaico/converter-ortomosaico.py`, Python/rasterio, sem precisar de GDAL nem compilador C++ instalado): GeoPackage do voo (SIRGAS 2000/UTM 22S) → `WarpedVRT` alinhada a EPSG:3857 → tiles WebP com alfa → MBTiles (SQLite, esquema TMS) → `pmtiles.convert()` → arquivo `.pmtiles` único. **PMTiles** foi escolhido (pesquisa real, fontes: docs.protomaps.com, maplibre.org) por hospedar em storage estático comum (Supabase Storage) via HTTP range requests, sem precisar de servidor de tiles dedicado rodando 24h. Achado real ao subir: Supabase Free rejeita upload de arquivo individual acima de ~50MB mesmo com o bucket configurado pra 100MB (é teto da plataforma, não do bucket) — resolvido comprimindo (WebP quality 78→62) sem perda visível, 53MB → 42,6MB. Bucket novo `mapas` criado (o `memoriais` só aceita image/video).

  **2 pontos de consumo, mesma camada compartilhada** (`lib/ortomosaico.ts`, `lib/registrarProtocoloPmtiles.ts`) — zero duplicação:
  - **"Como Chegar" de cada memorial** (`GuiaTumulo.tsx`) — só a camada de imagem entrou (via `useMemo` estendendo o estilo base), resto do componente **intocado** (regra 17 — Rafael autorizou explicitamente essa troca específica). Cemitério sem ortomosaico cadastrado continua no satélite ArcGIS genérico de sempre (fallback automático por `ortomosaico_url IS NULL`).
  - **Mapa novo em Central** (`/admin/cemiterios/[id]/mapa`, `components/admin/MapaCemiterio.tsx`) — pinos via `Layer` GeoJSON tipo `circle` (não `Marker` individual — aguenta milhares de túmulos sem travar), clique num pino abre popup com o memorial vinculado (se tiver) + atalho pras Gavetas 3D; clique num ponto vazio no "modo marcar" salva a coordenada exata direto em `lapides.latitude`/`longitude`. Cor do pino indica a origem da coordenada (dourado = marcado no ortomosaico, preciso; azul = GPS de celular/satélite genérico, impreciso).

  **Schema:** `cemiterios` ganhou `ortomosaico_url`/`minzoom`/`maxzoom`/`bounds`/`capturado_em` (coluna nula = fallback pro satélite genérico, nenhum cemitério existente quebra); `lapides` ganhou `coordenada_origem` (rastreia se veio de GPS impreciso ou clique preciso no ortomosaico). RPC `obter_localizacao_memorial` recriada (mudou tipo de retorno, `CREATE OR REPLACE` não bastava) com os `revoke execute` reaplicados na mesma migration — recriar função restaura `GRANT` implícito pra `PUBLIC`, mesma pegadinha já documentada nesta sessão com `acender_vela`/`incrementar_visualizacao`.
- [x] **Entrada do cemitério (âncora da rota) + card com foto no hover no mapa de Central** (2026-08-04) — `cemiterios` ganhou `entrada_latitude`/`entrada_longitude` (coordenada real do portão, separada do centro genérico do voo); botão "Marcar entrada do cemitério" em `MapaCemiterio.tsx`, mesmo padrão de clique-na-imagem do "modo marcar". `obter_localizacao_memorial` passou a devolver `coalesce(entrada, centro)` — a rota pública em `GuiaTumulo.tsx` (linha fixa + distância GPS) passa a começar do portão sem precisar mexer no componente (regra 17). Card com avatar (foto ou cruz) + nome ao passar o mouse num pino do mapa de Central, mesmo visual que já existia em `GuiaTumulo.tsx` mas faltava lá — via `onMouseMove`+query de feature, sem trocar o layer GeoJSON por `Marker` individual (mantém a decisão de escala de milhares de pinos).
- [x] **Sistema de endereçamento Quadra/Fila/Túmulo, independente de nome gravado** (2026-08-05, planejado com Opus depois de confirmar que nenhum arquivo do drone — GeoPackage/KMZ/zip, testado nos 3 — tem malha 3D ou foto oblíqua, só raster nadir; ler nome na pedra pela foto aérea é fisicamente impossível, ângulo de cima não mostra a face vertical) — sistema próprio de identificação que não depende de OCR nem de enxergar a gravação: **Quadra** (bloco, numerado a partir da entrada) → **Fila/Rua** (fileira dentro da quadra) → **Túmulo** (ordinal na fileira), código `Q03-R05-T012`. Motor real: staff clica só o início e o fim de uma fileira no ortomosaico (2 cliques) e `gerar_lapides_fila()` interpola N túmulos igualmente espaçados numa transação só — ~200-300 ações no total em vez de milhares de cliques individuais. Numeração de quadra/fila sugerida por distância até a entrada (`propor_numeracao_quadras`/`_filas`), com trava: recodificar túmulo já vinculado a memorial exige confirmação explícita (`p_confirmar_recodificacao`). `obter_geojson_cemiterio()` devolve quadras+filas+túmulos numa chamada só (RPC, sem o teto de 1000 linhas do PostgREST que um select paginado bateria com milhares de túmulos). Tabelas novas `quadras`/`filas` (RLS staff-all/parceiro-select, mesmo padrão de `lapides`); `lapides` ganhou `quadra_id`/`fila_id`/`numero`/`codigo`/`situacao`/`coordenada_precisao`/`foto_face_url` (gancho pra fase de nomes, ainda não construída de propósito — "depois pensamos nos nomes", pedido explícito do Rafael). Colunas antigas `identificacao`/`quadra`/`lote` de `lapides` viram espelho do código novo (não removidas, telas que já leem continuam funcionando). **Achado ao planejar, corrigido junto:** `app/admin/memoriais/[id]` e `app/parceiro/memoriais/[id]` carregavam **todas** as lápides de **todos** os cemitérios pro dropdown de vínculo, sem filtro nem limite — mesmo teto de 1000 linhas do PostgREST, silencioso (túmulo simplesmente some do dropdown sem erro). Corrigido pra carregar só as lápides do cemitério escolhido no próprio dropdown. **Pendente, mapeado no plano mas não construído ainda:** UI de campo mobile (`/admin/cemiterios/[id]/vistoria`) pro staff confirmar túmulo por túmulo andando no cemitério físico; LOD por zoom no mapa (hoje quadra/fila/túmulo sempre visíveis juntos, sem esconder por nível de zoom). Nenhum bloqueia o uso real — o cemitério José Lázaro já pode ser endereçado inteiro com o que existe. **Detalhe técnico completo (conceito, arquitetura, pipeline Python de reconhecimento por visão, bugs achados/corrigidos, status por cemitério) em `docs/mapa.md`** — anexo vivo, atualizar toda vez que essa tecnologia mudar (regra do Rafael, 2026-08-05).
- [x] **1ª quadra do José Lázaro mapeada de ponta a ponta + modo edição manual** (2026-08-05) — pipeline Python (`scripts/ortomosaico/mapear-cemiterio.py`) usado pelo agente pra reconhecer quadra/fileira por visão sem staff clicar em cada túmulo: **186 túmulos reais gravados no banco** (1 quadra, 10 ruas), confirmado com conferência visual (pontos lidos de volta do banco batendo com a foto original). 2 bugs reais achados e corrigidos no processo (sinal do ângulo invertido, fileira/corredor trocados na geração da malha) — ficam corrigidos no script genérico, valem pra qualquer cemitério futuro. **Modo edição manual** adicionado depois, a pedido do Rafael (pontos gerados por interpolação matemática não caem 100% em cima do túmulo real) — dentro de cada quadra, botão "Editar túmulos no mapa" troca a camada de milhares de pontos por pino grande numerado e arrastável (salva ao soltar), com adicionar/apagar túmulo e botão "Travar quadra" (`quadras.geometria_revisada`) pra travar contra edição acidental. Faltam ~7-8 quadras reais do José Lázaro (mapa geral já achou 12 candidatos, resto é pomar/telhado).
- [~] Deck do Pedro: 2 das 3 imagens abertas e analisadas 2026-07-24 (Página Memorial Pública, Aplicações Digitais/Institucionais) — gaps reais achados registrados abaixo em "Página do Memorial". Falta só "Presença Física e Comunicação".
- [ ] Redesenho de Dashboard + Gestão de Memorial em abas pra bater com o mockup do Pedro (escopo grande, não é tarefa de 1 sessão)
- [x] **Alerta de status do envio pro fornecedor da placa** (2026-07-30) — achado real ao investigar o pedido do Rafael: `configuracoes_sistema.email_fornecedor_placas` nunca tinha sido configurado (sempre `null`), então nenhum e-mail pro fornecedor jamais saiu, silenciosamente (zero registro em `emails_enviados`). Configurado com o e-mail do Rafael pra teste (`rvnegocioss@gmail.com`, via card "E-mail do fornecedor de placas" — ficava em `/admin`, desde 2026-09-17 fica na Central de Comunicações) — **trocar pelo e-mail real do fornecedor quando tiver**. Chip "Placa" na Central e no Portal do Parceiro agora reflete a fase real (sem mensagem/aguardando família/confirmada+enviada/erro no envio), puxando o último registro de `emails_enviados` tipo `envio_fornecedor` — antes só mostrava se a família confirmou, não se o e-mail de fato saiu. Confirmado também: família não tem campo pra escrever a frase da placa no próprio portal (`/familia/[slug]`) — só confirma via link de e-mail que a Central/Parceiro manda depois de escrever. **Bug real pego ao vivo no teste do Rafael:** com o e-mail do fornecedor recém-configurado, ele clicou "Gerar QR Code" manual num rascunho ainda com nome placeholder ("Novo memorial", nunca renomeado) — QR gerou e e-mail foi pro fornecedor com esse nome vazio (`emails_enviados` confirmou: assunto "QR Code para produção — Novo memorial", enviado). Corrigido: tanto o botão manual quanto o auto-disparo do primeiro save agora travam (Central: nome vazio; Parceiro: vazio ou ainda "Novo memorial") com aviso amarelo — QR ainda pode ser gerado com a página incompleta (foto/bio/timeline vêm depois, isso é normal), só o nome é obrigatório antes, porque o pedido pro fornecedor precisa saber de quem é a placa.
- [x] **Bug: "outra pessoa alterou" falso positivo ao editar nome** (2026-07-30, reportado ao vivo pelo Rafael) — trigger do banco `homenagens_set_updated_at` atualiza `updated_at` em QUALQUER escrita em `homenagens`, inclusive as "silenciosas" da própria ficha (gerar QR, cadastrar e-mail da família, salvar mensagem da placa, trocar "quem preenche o conteúdo"). Como só o "Salvar memorial" principal sincronizava o `updated_at` guardado na tela, usar qualquer uma dessas outras ações antes invalidava a trava de edição concorrente e bloqueava o próprio autor com a mensagem de "outra pessoa mexeu". Corrigido nos 2 lados (Central + Parceiro): as 4 rotas (`cadastrar-email-familia`, `salvar-mensagem-placa`, `memorial-qrcode`, e o update direto de `preenchido_por`) agora devolvem o `updated_at` novo, e cada handler já sincroniza o estado local na hora — não precisa mais recarregar a página entre uma ação e outra.
- [x] **QR Code e "Enviar acesso" da família agora exigem dado mínimo real** (2026-07-30, pedido direto do Rafael) — o e-mail de família também tinha saído como "Acesso ao memorial de Novo memorial" (confirmado em `emails_enviados`, mesmo bug do QR/fornecedor). Reforçado: QR Code (manual e auto no primeiro save) agora exige nome real **e** data de falecimento preenchida, não só nome; "Enviar acesso" da família exige nome real antes de disparar. Mensagem amarela explica o motivo em vez de falhar silencioso.
- [x] **Ficha do Parceiro sem campo de Cemitério/Lápide** (2026-07-30, achado real ao vivo pelo Rafael comparando com a página pública) — memoriais criados pelo Portal do Parceiro nunca tinham "Como Chegar" (mapa/rota) na página pública, porque não existia campo pra vincular lápide. Causa dupla: (1) UI realmente não tinha o campo (só a Central tinha); (2) RLS de `cemiterios`/`lapides` era 100% staff-only — o parceiro nem conseguia LER as próprias, mesmo `cemiterios.parceiro_id` já existindo. Corrigido: 2 policies novas de SELECT (`is_own_parceiro`) pra parceiro ler só os próprios cemitérios/lápides, campo Cemitério→Lápide (dependente) adicionado na seção Identificação da ficha do Parceiro, mesmo padrão da Central. Chip "Localização vinculada/Sem localização" na faixa de status do topo.
- [x] **Auditoria sistemática (Opus, 2026-07-30) — plano de verificação + 13 achados médios/críticos + 5 no dashboard** — pedido explícito do Rafael depois de 4 bugs achados só testando ao vivo: "monta um plano de verificação, não é tão complexo". Checklist reutilizável de 4 categorias criado (paridade de campo público×edição, dado mínimo antes de efeito externo, estado cliente desatualizado por trigger de banco, funcionalidade sem policy). **Achado crítico com risco real de perda de dado, corrigido na hora:** memorial criado pelo Portal do Parceiro nasce com slug `rascunho-<id>` e **nunca virava definitivo** em lugar nenhum do código, nem depois de preencher o nome real — e a limpeza automática de rascunho abandonado (`>2h`, roda a cada abertura de "Meus Memoriais") apaga qualquer coisa com esse padrão de slug, preenchida ou não. Um memorial de teste do Rafael (`jon doe da silva`) estava a horas de ser apagado sozinho; renomeado o slug manualmente pra tirar da faixa de risco, e corrigido na raiz: `salvar()` do Parceiro agora gera slug definitivo (`gerarSlugUnico`) no primeiro save com nome real, pra qualquer memorial novo daqui pra frente — o cleanup de 2h passa a fazer só o que devia (apagar rascunho de verdade abandonado). Também fechado: Central tinha o mesmo bug do "Novo memorial" indo pro QR/e-mail do fornecedor por um caminho que a correção de mais cedo não cobria (`load()` gerava QR sozinho só de abrir a ficha de um rascunho do Parceiro, sem clicar em nada) — mesma trava de nome+data aplicada lá. **Os 5 achados pendentes dessa auditoria foram corrigidos no mesmo dia (2026-07-30, itens já marcados `[x]` acima):** barra/quota de armazenamento 0MB, condolências sem moderação, card de pagamento decorativo, contagem de visita antes do gate, `/admin` sem paginação.
- [x] **"Clique pra conversar" (WhatsApp) na Central de Comunicações e no Portal do Parceiro** (2026-07-30) — Rafael queria WhatsApp de verdade dentro da Central (mensagem recebida aparecendo lá) — isso segue bloqueado esperando ele criar conta Google Cloud + Meta Business (API oficial, não dá pra contornar sem violar termo/arriscar banimento do número). Como alternativa legítima pra "facilitar agora": link oficial `wa.me` (`lib/linkWhatsApp.ts`) — abre o WhatsApp Web/app de quem já tá logado, direto na conversa com o número certo, zero API/conta necessária. Não traz mensagem recebida pra dentro do sistema, só atalho de abrir a conversa. Adicionado em `/admin/emails` (telefone do parceiro + telefone da família por memorial) e replicado em `/parceiro/emails` (nova seção "Contato das famílias", só existia a tabela de histórico de e-mail antes).
- [x] **Gaps da página pública vs mockup "Página Memorial Pública"** (achados 2026-07-24, feitos no mesmo dia) — barra de navegação no topo com botões "Deixar homenagem"/"Assinar livro"/"Compartilhar" (`BotaoCompartilhar.tsx`); "Livro de Assinaturas" como seção própria (reaproveita `condolencias`, assinatura em fonte cursiva); selos de confiança no rodapé. Card de QR Code na Localização foi feito e depois **removido a pedido do Rafael** (lugar errado). **Ainda falta:** badges de papel/vínculo perto do nome (Esposo/Pai/Avô/Amigo) — precisa campo novo no formulário, coluna `homenagens.vinculos` já existe no banco (migração aplicada 2026-07-24), só falta a UI.
- [ ] Razão social/CNPJ da própria Legado Digital (empresa em formalização) — placeholder nos Termos/Privacidade até sair
- [ ] Módulo financeiro completo (`contratos`, `planos`, `aquisicoes`, `fechamento_mensal`) — Fase 4
- [x] `SUPABASE_SERVICE_ROLE_KEY` **confirmado presente** nas env vars da Vercel (checado via CLI 2026-07-24, achado antigo estava desatualizado) — "Convidar contato" já funciona em produção.
- [x] **E-mail automático** (feito 2026-07-24) — trocado de Resend pra SMTP do Google Workspace (`lib/emailTransport.ts`, nodemailer). `contato@legadodigital.net` é um Grupo (sem senha própria); autenticação real via usuário técnico `sistema@legadodigital.net` com "Enviar como" `contato@` configurado. Variáveis já na Vercel. **DKIM + SPF + DMARC confirmados propagados** (2026-07-30, checado via `nslookup` real contra Google 8.8.8.8 e Cloudflare 1.1.1.1) — Pedro configurou o SPF (`v=spf1 include:_spf.google.com ~all`) no Hostinger depois do DKIM; DMARC já existia (`v=DMARC1; p=none`, configurado por ele antes). Autenticação de e-mail completa — nenhum bloqueio de DNS pendente.
- [x] **Layout responsivo da página do memorial** (mockup Claude Design, 2026-07-23) — container 520/1100px, hero lado a lado no desktop, seção "Acender uma vela" na posição do mockup (fim da página). Fonte mantida (Georgia) — decisão do Rafael, não trocou pro Playfair/Source Sans do mockup. Spec: `docs/superpowers/specs/2026-07-23-pagina-memorial-redesign-mockup.md`, plano: `docs/superpowers/plans/2026-07-23-pagina-memorial-layout-responsivo.md`.
- [x] **Integração Cemitério ↔ Parceiro ↔ Memorial ↔ Central** (2026-08-07, planejado com Opus a pedido do Rafael) — três achados reais confirmados no banco antes de mexer em código: (1) a página de lápides (`/admin/cemiterios/[id]/lapides`) era de antes do sistema Quadra/Fila existir, e o formulário manual dela era a fábrica de túmulo órfão — **achado grave**: 1 túmulo fora de fileira ("Jazigo Teste GPS") já tinha **3 memoriais diferentes vinculados**, exatamente o risco que o Rafael queria evitar (deixado como está por decisão dele, resolve depois pela ferramenta nova); (2) Portal do Parceiro nunca teve tela de cemitério/mapa — as RLS pra parceiro ler quadra/fila/lápide já existiam desde 30/07 e 05/08 mas **nenhuma tela usava isso**, e nenhum cemitério tinha `parceiro_id` preenchido (a permissão nunca teve efeito prático); (3) `cemiterios.parceiro_id` é 1:1 (um parceiro só) — nunca usado de verdade, e o modelo real precisa de vários parceiros no mesmo cemitério (cemitério municipal com várias funerárias atuando nele).

  **Banco** (aditivo, sem `drop`/`alter` destrutivo — a coluna `parceiro_id` antiga continua intacta, vira "operador principal" na prática): tabela nova `cemiterios_parceiros` (cemitério × parceiro × papel principal/operador × ativo), RLS própria (staff tudo, parceiro só as próprias linhas — senão a funerária A enumeraria quem mais opera no mesmo cemitério, vazamento comercial); helper `pode_ver_cemiterio()` (staff OU `parceiro_id` legado OU autorizado via `cemiterios_parceiros`); 4 policies novas em cemiterios/quadras/filas/lapides **acrescentadas** ao lado das antigas (policies permissivas se unem por OR no Postgres, nenhuma foi removida); `obter_geojson_cemiterio` migrado pro novo gate (revoke/grant reaplicados — recriar função restaura GRANT implícito pra PUBLIC, mesma pegadinha já documentada várias vezes). RPCs novas: `obter_arvore_lapides_cemiterio` (contagem por quadra/fileira + 3 categorias de "fora de fileira"), `obter_parceiros_do_cemiterio`/`obter_memoriais_parceiro_cemiterio` (agrupamento por parceiro dono), `vincular_lapide_a_fila` (conserta túmulo órfão, numera certo, guarda `codigo_anterior`).

  **`MapaCemiterio.tsx`** ganhou prop `modo?: 'edicao'|'leitura'` (default `'edicao'`, nada muda pra quem já usa) — em modo leitura, todo botão de escrita some (gate no container: nova quadra/fileira, gerar/duplicar/apagar/travar, editar túmulos no mapa, marcar entrada/avulso), mais uma guarda extra em `aoClicarMapa` (clique em leitura só seleciona pino, nunca escreve). Hover no pino mostra "Ocupado — outro parceiro" (sem nome/foto) quando o túmulo tem memorial de verdade mas não é visível pro parceiro logado — ocupação sim, identidade não, sem vazar dado de família/concorrente.

  **Página de lápides reescrita**: quadras lado a lado (colunas, ordem estável), fileiras retráteis dentro (cor igual ao mapa via `lib/coresFila.ts`, extraído do `MapaCemiterio.tsx`), túmulos como chips (carga em 2 níveis — árvore inteira é só contagem, túmulos de uma fileira só ao expandir ela). Seção "Fora de fileira" **sempre separada visualmente**, nunca dentro de uma coluna de quadra, com o grupo "já tem memorial" em vermelho no topo (o mais arriscado) mostrando os nomes. Formulário manual antigo não foi removido, só escondido atrás de "Avançado" com aviso de que cria túmulo órfão.

  **Select de lápide** (Central e Parceiro, os dois — achado do Opus: era o lugar real onde o erro de vínculo acontecia) virou `<optgroup>` por "Quadra N · Fileira M" mostrando o código real (`Q01-R02-T012`) em vez de só identificação livre numa lista plana de centenas de opções; túmulos fora de fileira caem no último grupo, rotulado como aviso. Escolher um túmulo arriscado (fora de fileira ou já vinculado) abre caixa vermelha com checkbox de confirmação obrigatório antes de salvar.

  **Painel "Quem opera neste cemitério"** abaixo do mapa na Central (`PainelParceirosCemiterio.tsx`) — memoriais agrupados por parceiro dono, bucket "Central / sem parceiro" pros sem `parceiro_id`, destaque pros que estão em túmulo fora de fileira.

  **Portal do Parceiro**: `/parceiro/cemiterios` (lista, RLS já filtra sozinha pra só os autorizados) + `/parceiro/cemiterios/[id]` (mapa em modo leitura + "meus memoriais neste cemitério", nunca os de outro parceiro) — item novo "Cemitérios" na nav.

  **Decisão consciente, não implementada** (mapeado pelo Opus, ficou pra depois): parceiro reportar problema no mapa sem editar geometria (`cemiterio_apontamentos`); unificar `lapides`/`lapidesEdicao` numa fonte única de verdade dentro do `MapaCemiterio.tsx`.
- [x] **Renomear quadra livremente** (2026-08-07, pedido do Rafael: "precisa bater com o que o cemitério tem lá, não ao contrário") — nome da quadra deixou de ser só o número sequencial de descoberta; UI de rename (mesmo padrão da fileira) grava em `quadras.nome`, exibido junto do número em toda a Central (`Q03` vira `Q03 — Alameda dos Ipês`, por exemplo).
- [x] **Sino do dash vira alerta real de comunicações** (2026-08-07, pedido do Rafael: "aonde esta o alarme tem que dar alerta de email, o que tiver de comunicações linkar no alerta") — o ícone de sino no header (`app/admin/layout.tsx`) era decorativo, sem `onClick`. Virou dropdown com o feed de `emails_enviados` (a mesma tabela que já alimenta `/admin/emails`, nenhuma tabela nova) — cada item linka pro memorial (`/admin/memoriais/[id]`) quando tem `homenagem_id`, senão pra Central de Comunicações; rodapé sempre linka pra lá. Badge vermelho só aparece com e-mail de status `erro` de verdade (nenhum ainda em produção). Rótulo de tipo de e-mail (`TIPO_EMAIL_LABEL`/`rotuloTipoEmail`) saiu de dentro de `admin/emails/page.tsx` e virou fonte única em `lib/emailLog.ts`, reaproveitada pelos dois lugares.
- [x] **Landing consolida acesso em "Área Restrita" + dash da Central ganha atalho de parceiros** (2026-08-07, pedido do Rafael) — `components/Navbar.tsx`: os 3 links soltos (Acesso Parceiros/Familiar/Legado Central) viraram um dropdown único "Área Restrita" (desktop e mobile), padrão de sistema em vez de poluir a nav. Dashboard da Central (`/admin`, canto superior direito do header em `app/admin/layout.tsx`): dropdown "Parceiros" pra staff pular direto pro Portal de qualquer parceiro (`/parceiro?parceiro_id=X`) sem abrir a ficha primeiro.
- [x] **Tema claro/escuro em toda a Central + todo o Portal do Parceiro** (2026-08-07/08-11, planejado com Opus, executado no mesmo dia) — começou restrito só ao Dashboard (`/admin`), o Rafael pediu pra estender: "isso seria na central inteira e do parceiro tb pode fazer se nao fica estranho". Toggle único no header dos dois portais (`lib/useTema.ts`, hook compartilhado — mesma pessoa cruza Central↔Parceiro via "Acessar Plataforma do Parceiro", tema não pode piscar na troca), aplicado em `document.documentElement` (não numa div interna — cobre modal/dialog, que renderiza via portal direto em `document.body`, fora da árvore do layout). Dark continua padrão, chave `legado-tema` no `localStorage` (lê a chave antiga `legado-central-tema` como fallback pra não perder o ajuste de quem já tinha trocado).

  **Achado real ao planejar** (por isso demorou 2 sessões): a hipótese óbvia — sobrescrever `--color-zinc-900` etc (já remapeadas pro navy desde o rebrand) sob um seletor `[data-tema=claro]` — **não funciona** nesse Tailwind v4/Turbopack. Testado dos dois jeitos (`@theme{}` sem `inline`, e `:root{}` puro): o build sempre manteve o cinza padrão do próprio pacote Tailwind, nunca o navy customizado — `@theme inline` (usado desde o rebrand original) grava o hex direto na utility em build-time, sem indireção nenhuma, e é por isso que funciona; sem `inline`, o valor do usuário perde pro `@theme default` interno do pacote por um motivo não totalmente esclarecido (não achei fonte oficial confirmando, só confirmado empírico direto no CSS gerado). Solução: tokens **novos**, fora do namespace `--color-*` do Tailwind (`--tema-zinc-950`...`--tema-zinc-200`, `--tema-blue-300/400`), declarados em `:root` puro sem `@layer` — esses o Tailwind não tenta reivindicar, então o mesmo truque que `.glass` já usa nesse arquivo (unlayered sempre vence `@layer theme/utilities`) funciona de verdade. Cada arquivo troca a classe `zinc-NNN` pela versão `[var(--tema-zinc-NNN)]` (`bg-zinc-900` → `bg-[var(--tema-zinc-900)]`) — testado que opacidade funciona igual (`bg-[var(--tema-zinc-900)]/50` gera `color-mix()` de verdade).

  **Conflito real do branco, resolvido por decisão de arquitetura, não gambiarra**: `text-white` é usado pra dois papéis diferentes — "texto de contraste sobre fundo neutro" (precisa inverter no claro) e "branco literal sobre botão colorido" (nunca muda). Resolvido sobrescrevendo a CLASSE `.text-white`/`.hover\:text-white`/`.file\:text-white`/`.ring-white` sob `html[data-tema=claro]` (nunca a variável `--color-white`) — os ~30 lugares de branco literal (botão dourado/verde/vermelho/âmbar/roxo, achados com grep, não chute) viraram `.text-branco-fixo` (classe nova, `color:#fff` sempre). `bg-white` (fundo do QR Code, 4 lugares) ficou **intocado de propósito** — nenhuma regra de tema chega nele, continua escaneável nos dois temas.

  **Escopo real, ~37 arquivos** (`app/admin/**`, `app/parceiro/**`, `components/admin/**`, mais `LegadoBotWidget.tsx`/`TrocarSenhaObrigatoria.tsx`/`badge.tsx`/`CemiterioMapPicker.tsx`, compartilhados pelos dois portais) — conversão mecânica (`sed`, não Edit manual arquivo por arquivo, dado o volume: ~925 ocorrências de classe zinc), cada arquivo com checagem própria de exceção de branco literal antes de seguir pro próximo. `LegadoBotPublicoWidget.tsx` (landing pública) e `components/familia/PrivacidadeFamilia.tsx` (Portal da Família) ficaram **de fora de propósito** — fora do pedido, e o atributo de tema nunca chega lá mesmo (só existe enquanto os layouts `/admin`/`/parceiro` estão montados). `--dash-*` (nome antigo, só do Dashboard) virou alias 1:1 dos tokens novos — `app/admin/page.tsx`/`SecaoRetratil.tsx` não precisaram trocar de nome.
- [x] **Ruas internas (caminho real) + rota calculada até o túmulo** (2026-08-07, planejado com Opus, executado no mesmo dia) — a rota da página pública ("Como Chegar") era sempre linha reta direto da coordenada geral do cemitério, ignorando os caminhos internos de verdade (podia cortar por cima de quadra). Tabela nova `ruas_cemiterio` (caminho/asfalto/calçada, existe no nível do cemitério, sem `quadra_id` — passa ENTRE quadras, diferente da fileira que fica DENTRO de uma quadra), desenhada em Central com o mesmo sistema de linha da fileira mas **quadrado roxo** no vértice (evita confundir com os marcadores já usados). Algoritmo próprio (`lib/rotaCemiterio.ts`, puro TS): monta a rede sozinho a partir das ruas desenhadas independentes (*noding* automático por interseção + detecção de encosto em T), roda Dijkstra pro caminho mais curto, e **nunca quebra** — sem rede ou rede longe demais, cai sozinho na linha reta de sempre. RPC `obter_rede_ruas_memorial` (só service_role) alimenta `app/homenagem/[slug]/page.tsx`, que calcula a rota no servidor e passa pro `GuiaTumulo.tsx` como prop opcional — regra 17 respeitada, só a coordenada da linha mudou, resto do componente intocado (diff conferido). Painel "Testar rota" em Central mostra a distância real pela rua vs linha reta antes de confiar. **Ainda sem efeito prático**: nenhum cemitério (incluindo José Lázaro) tem rua desenhada no banco ainda — a feature roda em fallback (linha reta) até o Rafael desenhar as ruas reais pela Central. Detalhe técnico completo em `docs/mapa.md`.
- [x] **Mapa Público de Cemitérios** (2026-08-13, planejado com Opus, executado no mesmo dia) — diretório público sem login, pedido pelo Rafael ("isso deixa igual já tá, só coloca público mesmo"). Fluxo hierárquico simples (`/cemiterios` lista cidade → `/cemiterios/[cidade]` lista cemitério → `/cemiterios/[cidade]/[cemiterio]` mostra o mapa daquele cemitério) — mesmo visual satélite+ortomosaico que já existia, só acessível sem staff/parceiro logado. Túmulo com memorial vinculado ganha pino discreto de **cruz** (nunca vela — regra 20, vela é elemento de "Acender uma vela", usar como pino sugeriria errado que clicar acende); cartão com nome+foto só aparece no hover/toque (decisão do Opus: cemitério com centenas de túmulo vira parede de rótulo sobrepostos com card fixo). Clique navega pra `/homenagem/[slug]`, nunca pro portal de edição da família. **Decisão consciente de privacidade, registrada pro Rafael antes de implementar**: o mapa é uma grade navegável nova (o oposto da decisão antiga de `/busca` "sem grade aberta, vazava privacidade") — nível escolhido foi mostrar nome+foto de todo memorial com gate `aberto` (o que o Rafael pediu), não só "tem memorial aqui" sem identidade. Sem toggle de privacidade novo: memorial aparece quando `busca_habilitada` + `link_habilitado` + `modo_gate≠oculto` já estão ligados (não só `link_habilitado` sozinho — uma família que desligou busca queria não ser listada, apareceria no mapa mesmo assim se o critério fosse só link). Rótulo do toggle de busca (`PrivacidadeMemorial.tsx`, compartilhado Central/Parceiro/Família) atualizado pra "aparece na busca por nome **e no mapa público de cemitérios**" — sem isso o consentimento vira ficção (regra 10). RPCs novas fechadas (`security definer`, `revoke` de `authenticated`/`PUBLIC`, só `anon` executa — inverso do padrão staff-only de sempre, mesma pegadinha de recriar função sempre reaplicada): `listar_cidades_publicas`, `listar_cemiterios_publicos`, `obter_mapa_publico_cemiterio` (achado real testando: 4 dos 5 memoriais de hoje não têm linha em `homenagens_seguranca`, `LEFT JOIN`+`coalesce` obrigatório, `INNER JOIN` teria devolvido mapa vazio sem erro nenhum). Filtro nunca deixa passar rascunho (`slug like 'rascunho-%'` ou nome vazio/`'Novo memorial'` — história real de rascunho órfão vazando, auditoria 2026-07-30) nem telefone/e-mail/parceiro_id/qualquer coluna de família. `cemiterios` ganhou `slug` (URL legível, backfill com desempate numérico) e `publico` (default `false` — só a Central liga quem entra no diretório, `/admin/cemiterios` ganhou o toggle, `/parceiro/cemiterios` só lê o chip, regra 22). Componente novo `components/public/MapaPublicoCemiterio.tsx` (não reaproveitou `MapaCemiterio.tsx` — 2627 linhas com ~30 funções de escrita, um `if` esquecido exposto ao público seria vazamento; nem props novas nele — mantém pequeno e auditável), reaproveitando só as libs já extraídas pra isso (`lib/ortomosaico.ts`, `lib/estiloSatelite.ts` novo, técnica de camada empilhada de `GuiaTumulo.tsx`, que fica **intocado**, regra 17). Zero chamada Supabase no client — Server Component busca com `supabaseServidor` e passa por props, mesmo padrão de `/homenagem/[slug]`. `/cemiterios` entrou no rate limit próprio (`proxy.ts`, categoria `mapa_publico`, 60/min — mais apertado que navegação normal porque é exatamente o vetor de raspagem que a decisão de nível A abre de propósito). Link "Cemitérios" na Navbar da landing e no rodapé de `/busca`.

- [x] **Ortomosaico do São Pedro processado e no ar** (2026-08-13, planejado com Opus, executado no mesmo dia) — segundo cemitério real com ortomosaico, primeiro usando o novo modo `--src-xyz` do `converter-ortomosaico.py` (pirâmide de tiles pronta em vez de GeoPackage, sem rewarp — achado: quando o processador do voo já entrega tiles renderizados, é melhor copiar direto que remontar raster e reamostrar de novo). **Achado crítico corrigido**: `cemiterios` do São Pedro estava com coordenada 373km errada (caindo em Goiás), já `publico=true` — o mapa público (`/cemiterios`) mostrava satélite vazio de lugar errado. Corrigido pra coordenada real tirada dos arquivos do drone. **Achado real sobre os arquivos entregues**: o formato óbvio (`.tif` soltos) era 16× pior que o real — o bom (`Sao Pedro map.zip`) só foi identificado inspecionando resolução de verdade, não pelo nome do arquivo. Resultado: São Pedro roda a **1,6cm/px** (2× mais nítido que o José Lázaro em produção, 3,5cm/px). Detalhe técnico completo, achado de escala (~10-15 mil túmulos estimados) e recomendação sobre o Tiled Model 3D do Metashape em `docs/mapa.md`.

- [x] **Numeração oficial da prefeitura + pontos de referência + foto do túmulo** (2026-08-14) — achado de campo (Pedro foi ao São Pedro): o cemitério **já tem numeração própria de quadra**, com mapa impresso da Prefeitura de Uberlândia na portaria e letreiro de ferro em cada esquina. Orientação do impresso validada com 2 provas independentes (GPS em cima da Quadra 36 batendo em "coluna 8 de 8, linha 5 de 10"; e os 4 letreiros fotografados dando a fileira 5 inteira na ordem **60 → 32 → 34 → 36 → 82**, idêntica ao impresso). Limite: o impresso é esquemático, dá a ORDEM certa mas nunca o tamanho real — por isso entrou tabela nova `pontos_referencia_cemiterio` (Capela/Administração/Sanitários/Velório, pino roxo arrastável no mapa de Central, RLS staff-all/parceiro-select) pra ancorar a grade do impresso na geometria real. E `lapides.foto_face_url` (coluna que existia desde 05/08 como gancho, nunca usada) foi ligada: staff sobe a foto do túmulo tirada de perto pelo popup do pino, e o **hover passa a mostrar foto + identificação mesmo sem memorial vinculado** — resolve o problema de fundo de que captura nadir de drone nunca mostra o nome gravado na face vertical da lápide. Mais a RPC `renumerar_quadra`: nosso `quadras.numero` nasce da ordem de desenho (1, 2, 3...), não do número real — o botão "# Renumerar quadra" troca pro número oficial, **regera o código de todo túmulo da quadra** (`Q02-R01-T005` → `Q36-R01-T005`, guardando `codigo_anterior`), troca de lugar com a quadra que já tinha aquele número (`UNIQUE(cemiterio_id, numero)` existe, faria falhar), e exige confirmação se algum túmulo afetado já tem memorial vinculado. Detalhe completo em `docs/mapa.md`.
- [x] **Cadastro de memorial direto no túmulo, em campo** (2026-08-14) — fluxo pedido pelo Rafael: staff no cemitério acha o túmulo e cadastra na hora, sem sair do mapa. **Botão direito no pino** abre menu (cadastrar memorial aqui / abrir memorial existente / foto do túmulo / mover pino / apagar); **botão direito num ponto vazio** marca um túmulo avulso ali. São 2 ações no túmulo (no botão direito e também no popup do clique esquerdo, onde antes só havia o texto morto "Sem memorial vinculado"): **Criar memorial aqui** — exige **só o nome completo** (mínimo pra existir), nasce com slug definitivo (nunca `rascunho-`, que a limpeza de 2h apagaria), já vinculado ao túmulo, abre a ficha completa em seguida e aparece na lista de Memoriais como qualquer outro; e **Vincular memorial existente** — busca por nome direto no banco (`ilike`, debounce 300ms, limite 15 — não carrega a tabela inteira, é a que mais cresce), avisando que vai **mover** se aquele memorial já estiver em outro túmulo (`lapide_id` é um só). Coluna nova `homenagens.origem_cadastro` (`central`/`parceiro`/`mapa_cemiterio`/`familia`, nullable) — sem ela não dá pra distinguir memorial vazio esperando alguém de memorial que nasceu assim de propósito. **Alerta de memoriais com dados faltando** (ficava no topo do Dashboard; desde 2026-09-17 mora na Central de Comunicações, `components/admin/PendenciasMemoriais.tsx`) lista os memoriais com dado faltando (data de falecimento, foto, história, contato da família), com link pra ficha e marca de "aguardando a família" (`preenchido_por`) / "cadastrado no cemitério". Lembrete do atalho fica no painel de Quadras do mapa.
- [x] **Foto = túmulo conferido em campo, com sinal visual nos 2 lados** (2026-08-14) — achado real: `lapides.situacao` tinha o estado `'confirmada'` desde 05/08 e a tela de Lápides já pintava contorno sólido pra ele, mas **nada no sistema nunca setava esse valor** (os 132 túmulos estavam todos `nao_confirmada`, a regra visual nunca disparava). Agora subir a foto do túmulo confirma ele (`situacao`/`confirmada_em`/`confirmada_por`, colunas que também já existiam sem uso) — leitura honesta, foto de perto só existe se alguém esteve lá; drone nunca produz isso. Efeito: pino fica **verde** no mapa (vence a cor de origem da coordenada), quadradinho na tela de Lápides ganha contorno sólido + bolinha verde, contador por fileira vira "N de M conferido(s)", e o popup do túmulo mostra a foto e traduz o código (`Q36-R05-T011` → "Quadra 36 · Fileira 5 · Túmulo 11"). Legenda dos 4 estados + passo a passo de como conferir escritos nas 2 telas — sem isso ninguém descobre a regra sozinho. Mais `pontos_referencia_cemiterio.geometria_revisada` (trava por ponto, mesmo nome de coluna de quadras/filas/ruas): sem ela o pino deslizava num arrasto acidental depois de já estar no lugar certo.
- [x] **1º memorial real do São Pedro cadastrado (Carlos Saraiva) + nome do memorial visível no painel de quadra** (2026-08-17) — dado oficial de sepultamento repassado pelo Rafael (Cemitério São Pedro, Quadra 36, Sepultura 11; nascimento 06/06/1922, falecimento 10/04/1995, filiação Gumercindo Saraiva Kappel) virou memorial de verdade (`carlos-saraiva`, `origem_cadastro='mapa_cemiterio'`, `preenchido_por='familia'`) vinculado ao túmulo `Q36-R01-T011` — o mesmo que o Pedro fotografou em campo. Sem linha em `homenagens_seguranca`, então cai nos defaults (aberto/busca/link) e já aparece no mapa público. **Achado ao cadastrar:** esse túmulo tinha foto mas continuava `situacao='nao_confirmada'` — a foto foi subida ANTES do deploy da regra "foto = conferido"; backfill aplicado (`update lapides set situacao='confirmada' where foto_face_url is not null`), 1 linha. **UI:** painel de Quadras do mapa e página de Lápides passaram a listar `Túmulo N — Nome do memorial` com link pra ficha, embaixo de cada fileira — antes o vínculo só existia como cor de chip e tooltip, não dava pra ler quem está onde sem passar o mouse túmulo a túmulo.
- [x] **Upload da família reescrito: arquivo não passa mais pelo Next** (2026-08-17, bug real reportado pela família Saraiva) — 3 defeitos encadeados, todos anteriores a esta sessão: (1) a Vercel corta requisição de função serverless acima de **~4,5 MB** e responde `Request Entity Too Large` em **texto puro** — o cliente fazia `res.json()` nisso e estourava com `Unexpected token 'R'`; o formulário prometia 10 MB de foto e 50 MB de vídeo, ou seja, **vídeo nunca funcionou em produção**; (2) a foto só era vinculada ao memorial quando alguém clicasse em "Salvar alterações" — como o salvar bateu em conflito, **11 arquivos ficaram órfãos no Storage** com `galeria_fotos` vazia no banco ("as fotos não aparecem"); (3) o falso "outra pessoa alterou este memorial": qualquer escrita na linha (inclusive as nossas edições por SQL no mesmo dia) mexe em `updated_at`, e a tela só sincronizava isso no salvar. **Correção:** upload virou 2 etapas — `/api/familia-upload/preparar` (valida sessão/quota/tamanho e devolve `createSignedUploadUrl`) → navegador manda **direto pro Storage** → `/api/familia-upload/confirmar` (lê os 16 primeiros bytes já no Storage pra validar magic bytes, apaga se não for mídia de verdade, **grava na coluna certa na hora** e devolve o `updated_at` novo). Regras compartilhadas extraídas pra `lib/uploadFamilia.ts`; detecção de MP4/MOV corrigida (assinatura fixa antiga falhava em arquivo de celular, que varia o tamanho do box antes do `ftyp`); uploads de galeria agora **sequenciais** (a rota lê e reescreve o array — em paralelo uma resposta sobrescrevia a outra); rate limit de 5 pra 10 arquivos/min. **Não se resolve com plano pago:** o teto de 4,5 MB é da arquitetura serverless, igual no Pro.
- [x] **Família define se a página é pública ou privada** (2026-08-17) — campo de senha de visita no card Privacidade do Portal da Família, com "Gerar senha" (alfabeto sem `0/O/1/l/I`, porque a senha vai ser ditada no grupo da família), "Redefinir" e "Remover proteção". A senha vale pros 3 canais juntos (link, busca e QR Code) e a tela diz isso — quem tem o QR tem o link, não dá pra trancar só um. `/api/memorial-senha` passou a aceitar o tripé staff/parceiro-dono/família via `autorizarMemorial`; a família só mexe em `senha_acesso_hash` (deixar ela trocar `senha_familia_hash` seria a sessão reescrevendo o próprio fator de login). `/api/familia-memorial` devolve só o booleano `temSenhaAcesso`, nunca o hash. **Decisão do Rafael:** a Central vê o estado e pode trocar/remover, mas nunca lê a senha da família — senha legível por staff deixa de ser prova de identidade (e é problema de LGPD).
- [x] **Portal da Família responsivo + card de privacidade simplificado** (2026-08-17) — a página era `max-w-xl` fixo: no monitor sobrava metade da tela vazia dos dois lados. Virou `max-w-6xl` com grid de 12 colunas (dados + linha do tempo na esquerda, privacidade e mídia na direita); no celular continua uma coluna. No mesmo dia, a pedido do Rafael, os **5 modos de portão saíram da tela da família** — ela controla só os 3 canais (busca/link/QR) e a senha; os modos com identificação/lista de e-mails continuam existindo, configurados por Central/Parceiro.
- [x] **Rota do "Como Chegar" visível de verdade** (2026-08-17) — a linha era dourada pontilhada fina e sumia sobre foto aérea (telhado claro, cascalho, grama seca). Virou linha sólida azul (#1D4ED8) com contorno branco embaixo, mesma técnica de rota do Google Maps/Waze — o contorno é o que garante leitura em qualquer fundo. Ponto "você está aqui" de 16 pra 22px. Regra 17 respeitada: só a aparência da linha e do ponto mudou. **Tentativa revertida no mesmo dia:** cheguei a fazer a rota recalcular a partir do GPS do visitante; o Rafael cortou ("era pra ser do portão"), revertido em `12344eb`. A rota da página pública **começa no portão**, ponto.
- [x] **Varredura de segurança + os dois baldes fechados** (2026-08-18) — pedido do Rafael ("tudo que for de segurança resolve primeiro"). **Corrigido:** (1) 3 vulnerabilidades de dependência, 2 altas (postcss com XSS e leitura de arquivo arbitrário via `sourceMappingURL`, nanoid) — Next 16.2.12 → 16.3.1, zero vulnerabilidades depois; (2) `slugificar()` sem `search_path` fixo, última função nessa condição; (3) **remover foto/vídeo não apagava nada** em nenhum portal — só sumia o link da tela, arquivo seguia ocupando cota de 500 MB e **acessível por quem tivesse a URL**; rota nova `/api/memorial-remover-midia` (tripé staff/parceiro/família, trava de caminho por memorial); (4) **balde `mapas` fechado** — ortomosaico do São Pedro (46,9 MB) era baixável por qualquer um que abrisse o mapa e olhasse a requisição; agora URL assinada de 12h (`lib/ortomosaicoAssinado.ts`, `/api/ortomosaico-assinado` pra Central/Parceiro); (5) **balde `memoriais` fechado** — o gate travava a PÁGINA mas as fotos da família ficavam abertas, ou seja, memorial "privado" servia mídia livremente. Toda mídia passa por `/api/midia/<caminho>` (`lib/urlMidia.ts`), que confere permissão **a cada requisição**. **Portão em vez de URL assinada de propósito:** assinatura é permissão congelada — depois que a família tranca o memorial, link assinado antes continuaria valendo até expirar. Detalhe que quase passou batido: `<img>` não manda cabeçalho de autenticação, então Central/Parceiro veriam imagem quebrada em memorial protegido — `/api/midia-sessao` troca a credencial do painel por cookie curto assinado (`lib/sessaoMidiaEquipe.ts`, 8h, só leitura de mídia). Foto de túmulo e QR Code ficam fora do gate (operacionais da equipe). **Validado em produção:** 8/8 (balde fechado nos dois casos, memorial aberto funciona, protegido recusa com 404 anti-enumeração, HTML sem nenhuma URL pública de Storage) + 10/10 na remoção de mídia (inclusive família tentando apagar arquivo de outro memorial → 403). **Tentativa revertida no mesmo dia:** liguei `security_invoker` na view `homenagens_busca_publica` pra zerar o único ERROR do scanner — **quebrou a busca pública na hora** (401), revertido em menos de um minuto. Aquele erro fica de propósito: pro modo que o scanner pede, o visitante precisaria de leitura na tabela de memoriais, o que exporia a coluna de hash de senha. **Pendente, ação manual do Rafael:** ligar "Leaked Password Protection" (Authentication → Providers → Email).

- [x] **Bateria de 24 cenários contra produção — 24/24, mais 3 furos que só apareceram testando** (2026-08-18, pedido do Rafael: "faz ai 20 cenarios de possiveis erros" / "achou corrige") — script em `scratchpad/bateria20.js` (acesso, upload, remoção, edição simultânea, presença, mídia, rajada de rate limit), rodado contra a produção real, não em mock. **Achados que a leitura de código não pegaria:**
  1. **Botão "Manter o meu e salvar" não gravava nada** (`53decbb`) — família clicava, nada acontecia, ficava presa no aviso de conflito pra sempre, justo a tela que veio consertar isso. *Stale closure*: `manterMeuTexto()` atualizava `valoresBase` pelo estado e chamava `salvar()` logo depois, que ainda lia o valor antigo — o servidor devolvia 409 de novo. Corrigido passando a base nova por parâmetro (`baseOverride`). Só pego abrindo no navegador de verdade: o defeito estava no botão, não na regra, então teste de API nunca acusaria.
  2. **429 na 3ª foto** (`ce0038f`) — o upload virou 2 requisições por arquivo (preparar + confirmar) quando o arquivo saiu de dentro do Next, mas o teto do `proxy.ts` continuou 5/min. Teto pra 24 (família) / 40 (staff); o limite real por arquivo continua em `lib/uploadFamilia.ts` (10/min), onde a regra de negócio mora.
  3. **Deploy fantasma** — `c8ff72d` foi commitado, pushado e está no GitHub, mas a Vercel **nunca buildou** (webhook não disparou). Eu estava tratando a correção de cache como resolvida em cima de um deploy que não existia. Só apareceu porque conferi o cabeçalho em produção em vez de confiar no commit. Forçado com commit vazio. **Lição operacional: `git push` verde não é prova de que subiu — conferir o comportamento em produção.**

- [x] **O CDN do Supabase servia arquivo já apagado (foto removida continuava acessível)** (2026-08-18, `dc7b16d`) — achado na bateria: subi um arquivo, apaguei do Storage (confirmado sumido na listagem) e o portão continuou entregando os mesmos 67 bytes, com `X-Vercel-Cache: MISS` — ou seja, não era a borda da Vercel. **Diagnóstico:** o CDN do Supabase (Cloudflare) responde `HIT` pra objeto já apagado e **ignora `Cache-Control: no-cache` tanto na resposta quanto no pedido**; o plano Free usa CDN básico, sem invalidação ao deletar (o Smart CDN, que invalida em ~60s, é do Pro). O único jeito de ver a verdade é query nova na URL (`cf=BYPASS` → 400). O portão estava certo; o andar de baixo mentia pra ele. **Duas camadas, porque uma só não basta:** (1) o arquivo precisa **ainda pertencer** ao memorial — aceitar qualquer caminho embaixo do id significa que foto removida da página segue sendo entregue pra quem guardou a URL, e que arquivo órfão (subido e nunca vinculado) também; como a remoção tira a mídia do banco **antes** de apagar o arquivo, a decisão da família passa a valer no mesmo instante, sem depender de cache expirar. Equipe e família passam sem essa checagem **de propósito**: na Central e no Portal do Parceiro a foto vai pro Storage e só entra no banco no Salvar, então exigir vínculo ali quebraria a prévia antes do save. (2) busca no Storage com cache-buster em vez do SDK, pra que arquivo apagado responda 404 também pra equipe e família. **Validado em produção:** arquivo órfão barrado (404), e o fluxo real ponta a ponta — família sobe pelo caminho de 2 etapas, visitante vê, família remove, a mesma URL cai pra 404 em 3 tentativas seguidas.
  **Achado de faxina, não corrigido ainda (decisão do Rafael):** 12 arquivos órfãos no balde `memoriais`, 10,6 MB — 5 no memorial de teste e **7 no Carlos Saraiva**, resíduo do bug de upload de 17/08 (o Pedro subiu 11 fotos, só 4 cabem na galeria). Estão barrados pelo portão desde este commit, mas seguem ocupando a cota de 500 MB. Três delas são fotos distintas que nunca entraram (`2024-10-05 (4)`, `2024-10-05 (5)`, `2024-10-06 (3)`) — apagar é destrutivo e irreversível (regra 1), então fica aguardando decisão: apagar tudo, ou reaproveitar as 3 distintas antes.
- [x] **Trava de edição simultânea + auto-save no Portal da Família** (2026-08-18) — bug real que fez a família Saraiva perder a linha do tempo duas vezes: `homenagens` tem trigger que carimba `updated_at` em qualquer escrita, e a checagem comparava a **linha inteira** — "alguém anexou uma foto" dava o mesmo 409 que "alguém reescreveu a biografia", e a única saída oferecida (recarregar) apagava o texto não salvo. Quatro camadas novas: (1) conflito **por campo** (`lib/conflitoEdicao.ts`, campo igual ao carregado é descartado antes da checagem e da escrita); (2) **auto-save** 3s após parar de digitar, com mínimo de 8s entre gravações; (3) **presença ao vivo** (`memorial_edicao_sessoes` + `lib/useTravaEdicao.ts`), avisando nos dois lados antes de digitar; (4) **rascunho local** no navegador, restaurado com aviso. Conflito real deixou de ser beco sem saída: mostra as duas versões com "Manter o meu" / "Usar a que está salva". **Travado contra regressão:** 6 testes (`node --test`, sem dependência nova) rodando no CI a cada push + `scripts/simular-conflito-familia.js` (5 cenários contra produção). A simulação pegou bug que a 1ª correção não resolveu — 2 de 5 falhavam, incluindo o caso do Pedro. Rate limit próprio `edicao_familia` (90/min) porque o auto-save estourava o limite genérico de `/api`.
- [x] **Login da família volta a ser só senha + e-mail real disparado pro Pedro** (2026-08-17, pedido direto do Rafael: "acesso só com a senha que vai encaminhada pro email") — a tela pedia e-mail **e** senha desde a auditoria de 29/07; o Rafael cortou o campo de e-mail: a senha já é entregue no e-mail cadastrado, então exigir o endereço de novo na tela repetia o mesmo fator e travava quem não lembrasse qual e-mail a funerária cadastrou. `/api/familia-login` deixou de ler `familia_email` (nem seleciona mais a coluna) e responde "Senha incorreta"; a tela ganhou a linha "A senha foi enviada por e-mail quando o memorial foi cadastrado". Trava contra força bruta continua sendo o rate limit de login do `proxy.ts` (3/min por IP). No mesmo dia o acesso do Pedro Saraiva ao memorial do avô (Carlos Saraiva) foi enviado de verdade por e-mail.
- [x] **Conferência de túmulo por coordenada de GPS de campo** (2026-08-17) — painel novo no mapa do cemitério (Central): staff cola o link do Google Maps (ou os números) que alguém mandou de dentro do cemitério, cai um pino laranja e o mapa mostra o túmulo mais perto + o desvio em metros. `lib/parseCoordenada.ts` lê 5 formatos e **prioriza o pino real (`!3d/!4d`) sobre o `@lat,lng`** do link do Google — o `@` é o centro da tela quando o print foi feito, quase sempre alguns metros deslocado do pino (erro fácil de não perceber). Colunas novas `lapides.gps_referencia_latitude/longitude/em/nota` guardam a medição de campo **sem nunca sobrescrever** a coordenada do ortomosaico; o botão "Mover túmulo pra cá" existe mas avisa que o ortomosaico é mais preciso (2,2 cm/px contra 3-5 m do GPS de celular). Passo a passo do procedimento de campo escrito dentro do próprio painel (regra 10). **Primeira validação real:** o ponto que o Pedro mediu no São Pedro caiu a **0,62 m** do Q36-R01-T011 (vizinho a 1,35 m) — guardado como referência do túmulo. Confirmação não vem do GPS sozinho (erro do aparelho é maior que a distância entre túmulos vizinhos), vem do cruzamento de 4 fontes: registro oficial da prefeitura (Quadra 36, Sepultura 11) + contagem na fileira do ortomosaico + foto de campo + GPS.

- [x] **4 erros achados testando ao vivo, todos silenciosos** (2026-08-26) — nenhum aparecia como erro na tela; os quatro falhavam calados.
  1. **Ortomosaico sumido no mapa da Central** (o mais grave). `MapaCemiterio.tsx` lê `ortoUrlAssinada` dentro de um `useMemo` cuja lista de dependências era só `[cemiterio]`. A URL assinada chega numa segunda requisição, sempre depois do cemitério — então o memo nunca recalculava e o mapa ficava preso na URL pública gravada em `cemiterios.ortomosaico_url`, que responde **HTTP 400** desde que o balde `mapas` virou privado (18/08). Efeito: satélite, ruas e pinos apareciam, ortomosaico nunca; no zoom alto sobrava o placeholder cinza "Map data not yet available" da Esri (World_Imagery não tem cobertura em z≥20 nessas cidades). **Os arquivos estavam intactos** — confirmado puxando tile real de z18 a z23 dos dois `.pmtiles` antes de mexer em código. `GuiaTumulo.tsx` e `MapaPublicoCemiterio.tsx` não tinham o defeito (recebem a URL já assinada no servidor, e a lista de dependências deles está completa). **Achado junto:** `react-hooks/exhaustive-deps` não está ativo neste projeto (`npm run lint` passa limpo) — nada no CI pega essa classe de bug.
  2. **Logo do parceiro invisível e impossível de remover.** Mesmo estrago do balde privado: `subirLogo()` monta `getPublicUrl` e as 3 telas que mostram logo (Central, Portal do Parceiro, página pública `/parceiros/[slug]`) exibiam link morto. `parceiro-logos` entrou na lista de pastas sem gate de memorial em `/api/midia` (junto de `tumulos` e `qrcodes` — logo comercial não pertence a memorial nenhum) e as 3 telas passaram a usar `urlMidiaProtegida()`.
  3. **Dropdown "Parceiros" do header da Central sempre vazio.** A consulta pedia `select('id, nome')` e `parceiros_b2b` **não tem coluna `nome`** (tem `nome_fantasia` e `razao_social`) — o select falhava, `data` vinha `null` e a tela dizia "Nenhum parceiro cadastrado ainda" com 3 parceiros no banco. Agora lê as duas colunas, cai pra razão social quando o fantasia é vazio, e loga o erro no console em vez de engolir.
  4. **Aviso de slug desatualizado** na ficha do parceiro ("rode a migração de backfill antes de publicar") — mandava fazer uma coisa que não existe mais desde que o slug passou a ser gerado no save (regra 4).

  **Rota nova `/api/remover-arquivo`** — remoção de arquivo dos campos que **não** são mídia de memorial (essa já tem `/api/memorial-remover-midia`, com o tripé staff/parceiro/família). É um **registro, não uma rota por campo**: campo de arquivo novo entra acrescentando uma linha em `RECURSOS` (tabela, coluna, pasta, quem autoriza, colunas extras a zerar). Hoje cobre `logo_parceiro` (staff ou o próprio parceiro, regra 22) e `foto_tumulo` (só staff — geometria de cemitério é compartilhada entre parceiros). A URL apagada sempre vem da linha do banco, nunca do corpo da requisição, então não dá pra mandar caminho de outro dono; a trava de pasta é defesa em profundidade. **`foto_tumulo` devolve o túmulo pra `nao_confirmada`** ao remover: a foto de perto é a única prova de que alguém esteve lá (drone nadir nunca mostra a face gravada), então pino verde sem foto seria o mapa afirmando o que não sustenta mais. Helper novo `lib/autorizacaoEquipe.ts` (`identificarChamador`) — o mesmo bloco vivia solto dentro de `lib/autorizacaoMemorial.ts`, que só serve pra memorial; agora rota que mexe em coisa fora de memorial tem onde se apoiar sem copiar. Checa `usuarios.ativo` também (desativar alguém na Central não tirava o acesso dele às rotas de API).

- [x] **Domínio `legadodigital.net` no ar** (2026-08-26) — o domínio apontava pro parking da Hostinger desde sempre; a Vercel tinha **0 domínios** configurados. Pedro compartilhou o acesso da conta Hostinger (não transferiu titularidade — os 3 domínios seguem no nome dele). Ordem que importa: cadastrar na Vercel **primeiro** (`vercel domains add`), senão o DNS aponta pra um projeto que não reconhece o domínio e o site cai. **Achado ao cadastrar:** a Vercel não pede mais `76.76.21.21` (valor antigo que eu ia passar de memória) — pede `216.198.79.1`/`64.29.17.1` no apex e um CNAME **específico do projeto** (`acd51ad49bda37fb.vercel-dns-017.com`) no `www`; não tinha como saber sem cadastrar antes. A Hostinger recusa dois registros `A` no mesmo nome, então ficou só o primeiro (o segundo era redundância). Nameservers **não** foram trocados de propósito — `solar`/`lunar.dns-parking.com` é onde vivem MX/SPF/DKIM/DMARC do Google Workspace, e trocar apagaria tudo. Zona inteira salva antes de mexer em `Desktop\Cerebro Claude - Legado Digitalackups\zona-dns-legadodigital.net-2026-08-26.txt`. Validado depois: apex e `www` em 200 com HTTPS, `/homenagem/[slug]`, `/familia/login` e `/admin` respondendo, MX intacto.

- [x] **Auditoria completa de verificação (04/09/2026, checklist retomado do zero, 3 agentes em paralelo, só leitura de código+banco)** — achados reais corrigidos no mesmo dia:
  - **Convite de parceiro/staff sobrescrevia conta existente** (achado 26/08, agora corrigido) — `convidar-parceiro` e `convidar-usuario` não recusavam e-mail que já tinha conta: `updateUserById` trocava a senha e o papel de **qualquer** conta com aquele e-mail (staff, parceiro), cruzando a hierarquia da regra 22. Corrigido nos 2: bloqueia com erro 409 se o e-mail já pertence ao "outro lado" (staff↔parceiro).
  - **Convite de staff usava senha fixa `123456`** (6 caracteres, abaixo do mínimo de 10 do Supabase Auth — convite provavelmente falhava sempre, silenciosamente) — corrigido pra senha aleatória de 12 caracteres, mesmo padrão já usado no convite de parceiro (`lib/gerarSenhaTemporaria.ts`). Resposta da API também não devolvia `email`/`tempPassword` que a tela já esperava — corrigido junto.
  - **Operador Legado Digital conseguia se auto-promover a Admin** — RLS de `usuarios_perfis` usava `is_legado_staff()` (verdadeiro pros 2 papéis) pra liberar escrita. Nova função `is_admin_legado()`, escrita em `usuarios_perfis` agora exige Admin.
  - **Escritas públicas (condolência, mural, vela) não conferiam o portão de privacidade no servidor** — só rate limit. Um memorial com senha/oculto ainda aceitava spam se alguém pegasse o `memorialId` do HTML antes de passar pelo gate. Novo `lib/verificarGatePublico.ts`, chamado nos 3 endpoints antes de qualquer insert, reusando a mesma lógica (`resolverAcesso`) e cookie que a página pública já usa.
  - `admin_dashboard_metricas` (RPC do dashboard) estava executável por `anon` — revogado, só `authenticated` agora.
  - Todo o resto do checklist (Central, Portal do Parceiro, Portal da Família, página pública, busca/mapa público, segurança transversal) confirmado por código — **sem regressão desde 28/08**. Detalhe completo em `docs/CHECKLIST_VALIDACAO_2026-08-28.md`.
  - **Achado, não corrigido (baixa prioridade):** painel de quadras do `MapaCemiterio.tsx` em modo leitura (Portal do Parceiro) não esconde link "meus memoriais" que aponta pra rota só-staff — link morto pro parceiro, não vaza dado (destino é protegido), só UX quebrada.

## Portal do Parceiro B2B — como funciona
Cada funerária/parceiro tem acesso próprio, fora da Central, vendo só os próprios memoriais.
1. Papel **"Parceiro B2B"** semeado em `perfis`; tabela `parceiros_usuarios` (usuario_id, parceiro_id) permite mais de 1 pessoa por funerária
2. RLS em `homenagens`: `homenagens_parceiro_own` restringe ao próprio `parceiro_id` via `is_own_parceiro()`; `homenagens_staff_all` mantém acesso total pra Admin/Operador
3. `/parceiro/login` + `/parceiro/layout.tsx` — protegido, papel Parceiro B2B
4. `/parceiro` — dashboard (total de memoriais, plano contratado, status de pagamento)
5. `/parceiro/memoriais` — "+ Novo Memorial" cria o rascunho na hora e já leva direto pra ficha completa em `/parceiro/memoriais/[id]` (2026-07-29 — uma tentativa de separar em cadastro curto + ficha foi revertida no mesmo dia a pedido do Rafael: ele queria só melhor organização visual, não um passo a mais antes de chegar no memorial). **Layout revisado (2026-07-29, planejado com Opus + pesquisa de referência real — Linear/Notion/Attio/HubSpot/Stripe/Airtable/Salt Design System):** grid de 12 colunas (`lg:grid-cols-12`, conteúdo `col-span-8` + operacional `col-span-4`, era `grid-cols-2` sem breakpoint — travava em 2 colunas do celular ao monitor grande); faixa de status no topo (6 indicadores: público/senha, acesso da família, quem preenche, placa, galeria, armazenamento); seções sempre visíveis por assunto (Identificação, História, Galeria e vídeo, Linha do tempo) — nada mais escondido atrás de retrátil fechado por padrão; campo do tamanho do conteúdo esperado (data não ocupa a largura de um nome); labels em `text-zinc-400` (contraste corrigido, `zinc-500` reprovava AA); botões com o que fazem no nome ("Salvar memorial", "Definir senha", "Enviar acesso"). Componentes novos reaproveitáveis: `components/admin/CampoFicha.tsx`, `SecaoFicha.tsx`, `StatusFicha.tsx`. **Cadastro da família** ganhou campos "Nome do responsável" e "Telefone" ao lado do e-mail já existente (colunas `homenagens.familia_nome_responsavel`/`familia_telefone`, API `cadastrar-email-familia` atualizada) — mesmo fluxo de sempre (gera senha, manda por e-mail, família usa pra entrar no Portal da Família e subir fotos), só captura mais dado de contato do responsável. Mesmos 2 campos também na Central. **Unificado 2026-09-16:** a Central usa a mesma ficha (`components/admin/FichaMemorial.tsx`), em abas. Container do Portal ainda `max-w-7xl` (não alargado — decisão do Rafael pendente).
6. Botão "Convidar contato" em `/admin/parceiros/[id]` → `POST /api/admin/convidar-parceiro` (service role, server-side) — cria usuário com senha gerada por parceiro (`lib/gerarSenhaTemporaria.ts`, não mais fixa), vincula papel+parceiro, exige troca de senha no 1º login (`usuarios.senha_temporaria`)
7. **Acesso direto da Central**: botão "Acessar Plataforma do Parceiro" na ficha leva a equipe interna pro Portal do Parceiro daquele parceiro (`/parceiro?parceiro_id=X`) sem logar de novo — aviso "Visualizando como: X — modo Central"
8. **`/parceiro/cemiterios`** (2026-08-07) — mapa dos cemitérios autorizados em modo leitura (`MapaCemiterio` com `modo="leitura"`, nunca edita geometria compartilhada com outros parceiros) + "meus memoriais neste cemitério". Autorização vem de `cemiterios.parceiro_id` (legado, 1 parceiro) OU da tabela `cemiterios_parceiros` (N:N, vários parceiros no mesmo cemitério — ver "Integração Cemitério ↔ Parceiro ↔ Memorial ↔ Central" na Fase Atual).

## Busca pública e privacidade por senha — como funciona
`/busca` e `/parceiros/[slug]` usam `components/public/BuscaMemorial.tsx`: busca por nome, sem grade aberta (vazava privacidade). Resultado sem senha mostra link direto; com senha, pede senha antes.

Busca **sem sensibilidade a acento** via `buscar_homenagens_publicas(termo, p_parceiro_id)` (extensão `unaccent`), chamada via `supabase.rpc()`.

**Arquitetura de segurança:** senha nunca fica em `homenagens` (leitura pública restrita a colunas seguras, ver abaixo). Fica em `homenagens_seguranca` (hash scrypt, salt = id do memorial), sem policy de leitura pública. View `homenagens_busca_publica` expõe só booleano `tem_senha`. Verificação/escrita passam por API routes server-side (`/api/memorial-acesso` público, `/api/memorial-senha` autenticado) com service role key — hash nunca trafega pro client.

**Leitura pública de `homenagens` (2026-07-29):** view `homenagens_publica` (13 colunas explícitas — as que `app/homenagem/[slug]/page.tsx` de fato usa) substitui o GRANT genérico que existia antes pra `anon`. `familia_email`/`familia_nome_responsavel`/`familia_telefone`/`mensagem_placa`/`preenchido_por`/`updated_at` nunca saem daí. `parceiros_publicos` (usada em `/parceiros/[slug]`) segue o mesmo padrão pra `parceiros_b2b`.

**Achado real (Opus, 2026-07-30, investigando o plano dos modos de privacidade completos):** `security_invoker=on` de `homenagens_publica` (linha acima) tinha sido setado em 2026-07-29 mas foi **derrubado silenciosamente** por um `create or replace view` posterior (migration de vínculos, mesmo dia) — `create or replace view` não preserva reloptions, então qualquer edição futura na view precisa reaplicar `alter view ... set (security_invoker = on)` depois. Mais grave: `/homenagem/[slug]/page.tsx` (Server Component) lia com a chave **anon** (pública, embutida em todo navegador) em vez de service role — como a policy de leitura pública de `homenagens`/`homenagens_publica` segue liberada pra `anon` (é assim que a página funciona sem login), qualquer um com a slug conseguia ler o memorial inteiro direto via REST do Supabase **ignorando senha, busca/link/QR desabilitado e tudo mais** — o gate só bloqueava a tela, não o dado. Impacto real hoje é zero (nenhum memorial em produção usa senha), mas era teatro de segurança esperando um memorial de verdade usar. Corrigido: a página passou a ler com `lib/supabaseServidor.ts` (novo, service role, guard de runtime contra import em client component); `generateMetadata` também vazava nome/foto no `<title>`/OpenGraph antes de checar senha, corrigido junto. **Fechado (2026-07-30):** `SELECT` de `anon` revogado em `homenagens`/`homenagens_publica`/`condolencias`/`mural_memorias`, `security_invoker=on` reaplicado em `homenagens_publica` — testado como role `anon` direto no banco, as 4 leituras bloqueadas (`permission denied`), busca (`homenagens_busca_publica`) continua funcionando normal. Durante a correção, o conector remoto do Supabase MCP (`claude.ai Supabase`) caiu no meio da sessão e não reconectou — resolvido trocando pra um **MCP local do Supabase** (`~/.claude.json`, chave `supabase`, roda `@supabase/mcp-server-supabase` instalado em `~/.claude/local-mcp/supabase/`, autenticado com um Personal Access Token da conta — guardado em `Knowledge/Legado Digital - Credenciais Tecnicas.md` no vault). `get_advisors` rodado depois da correção: 1 achado ERROR pré-existente e já aceito (`homenagens_busca_publica` sem `security_invoker`, decisão documentada — precisaria expor hash de senha pra virar invoker), resto é WARN de baixa prioridade (search_path de função, extensão `unaccent` no schema public, leaked password protection do Auth desligado) — não tocados, fora do escopo desse achado.

**A senha bloqueia os DOIS caminhos:** busca E link/QR direto — hoje é só um dos 5 modos de portão possíveis (`modo_gate = 'senha'`), resolvidos por `lib/modosPrivacidade.ts` (`resolverAcesso`). Detalhe completo dos 5 modos (com identificação, com allowlist de e-mail, oculto) no item "Modos de privacidade completos" da Fase Atual. Cookie assinado HMAC `mem_acesso_{slug}` (30 dias pra senha, 7 pra cadastro/email), payload versionado (`modo` + `gate_versao`) — trocar o modo derruba cookie na hora.

Definir/trocar/remover senha: campo "Senha de acesso" no formulário (Central `/admin/memoriais/[id]` e Portal do Parceiro `/parceiro/memoriais`) — em branco = memorial público.

## Portal da Família — como funciona
**1 e-mail de contato por memorial, sem conta** (modelo simplificado — antigo responsável+código pra até 3 parentes foi removido):
1. Central/Parceiro cadastra nome do responsável, e-mail e telefone da família na ficha (`homenagens.familia_nome_responsavel`, `familia_email`, `familia_telefone`) → `POST /api/admin/cadastrar-email-familia`
2. Sistema gera senha simples (8 caracteres hex), hasheia em `homenagens_seguranca.senha_familia_hash`, manda por e-mail (SMTP Google Workspace)
3. Família busca pelo nome do homenageado em `/familia/login` (nunca por slug) + **só a senha** via `POST /api/familia-login`, cookie HMAC de 12h que revoga na hora se a senha trocar. O campo de e-mail foi exigido entre 2026-07-29 e 2026-08-17 e **removido a pedido do Rafael** ("acesso só com a senha que vai encaminhada pro e-mail") — a senha já chega no e-mail cadastrado, então pedir o e-mail de novo na tela repetia o mesmo fator e travava família que não lembrava qual endereço foi cadastrado. Contra força bruta quem responde é o rate limit de login do `proxy.ts` (3/min por IP)
4. Dentro de `/familia/[slug]`, edita os mesmos campos do admin/parceiro (foto, vídeo, galeria, timeline, bio, frase) — grava direto em `homenagens`

Se o SMTP falhar (ex: DKIM não propagado ainda), a API retorna a senha na resposta pro staff/parceiro repassar manualmente.

**Ainda falta:** "esqueci a senha" da família (hoje só reemissão manual).

**Login de memorial escondido (corrigido 2026-09-16, item da ata de sócios de 16/07).** A busca da tela de login usa `buscar_homenagens_publicas`, a mesma da busca pública, que filtra `busca_habilitada = true and modo_gate <> 'oculto'` — então a família que escondia o próprio memorial deixava de encontrá-lo pra entrar, mesmo sabendo a senha. `/api/familia-login` passou a aceitar **`nome` + `senha`** além do `slug`: nesse caminho procura por nome **sem filtro de privacidade** e confere a senha de cada candidato (homônimo é caso real — entra no que a senha abrir), ignorando rascunho. **Anti-enumeração:** nome errado, senha errada e memorial escondido devolvem a mesma frase, então ninguém descobre memorial oculto testando nomes ali; pelo `slug` a mensagem continua "Senha incorreta", porque nesse caminho a pessoa já sabia qual memorial era. Força bruta segue barrada pelo rate limit de login do `proxy.ts` (3/min por IP, que já cobria a rota). Na tela, o campo de senha direto só aparece quando a busca não acha nada.

**Estrutura da tela — abas, um tema por vez (2026-09-16).** A página era tudo empilhado numa coluna sem fim; virou **barra de abas no topo**: `Memoriais · Fotos e vídeos · Jazigo · Árvore · Privacidade · Assinaturas`. Cada aba abre sozinha na largura toda, e **dentro da aba não existe retrátil** — foi assim que o "tá tudo enorme" se resolveu: nada mais compete por altura na mesma tela. O Jazigo aparece junto da aba Memoriais (é o contexto da família) e também tem aba própria. Antes de chegar aqui foram testadas e descartadas 2 tentativas: 2 colunas com Fotos preenchendo o vazio da direita, e acordeão com tudo retrátil — o Rafael reprovou as duas por continuarem longas.

**Identidade é da família, não do homenageado.** Topo mostra "PORTAL DA FAMÍLIA" + o nome do jazigo (`lapides.nome`, ex: "Jazigo Família Saraiva"); sem nome ainda, cai em "Família de [nome do homenageado]". A seção **Memoriais** lista a família inteira do jazigo com retrato em esfera (anel dourado em gradiente cônico, mesmo padrão do card do mapa) — quem está sendo editado em destaque, os outros linkam pra própria página pública.

**Campo "Família (pode deixar em branco)"** no formulário grava no **mesmo dado que nomeia o jazigo** (`lapides.nome`), via `POST /api/familia-jazigo` no `onBlur`. Não existem dois nomes de família concorrendo — são dois lugares de editar o mesmo campo (o outro é o card do Jazigo).

**A família edita o jazigo** (mudou 2026-09-15/16, antes era só visualização): dá nome ao jazigo e escreve quem está enterrado nas gavetas que ainda não têm memorial. **Criar/remover gaveta e mexer em gaveta que já tem memorial continuam só do parceiro/staff** — gaveta com memorial pode ser de outra família (regra 22). A trava está no servidor (`/api/familia-jazigo` POST): só o jazigo do próprio memorial (lido de `homenagens.lapide_id`, nunca de id mandado no corpo), só gavetas daquele jazigo, só gaveta sem `homenagem_id`.

**Assinaturas não renderiza mais o livro** — é só a lista de quem assinou com "remover" do lado (`DELETE /api/memorial-condolencia`). O livro desenhado continua na página pública do memorial.

**Modo ajuda** (`components/familia/ModoAjuda.tsx`) — botão no cabeçalho que liga as dicas de uma vez; ligado, cada campo ganha um "?" dourado que mostra um card explicando pra que serve (hover no desktop, toque no celular). Desligado (padrão), a tela fica idêntica, sem ícone nenhum. A escolha fica lembrada no navegador (`localStorage`, chave `legado-modo-ajuda`).

**Bug corrigido 2026-09-16 — aviso falso de "rascunho recuperado":** o próprio carregamento da página (`setForm` com os dados do servidor) disparava o autosave de rascunho local, gravando no `localStorage` um rascunho idêntico ao que já estava salvo. Na visita seguinte isso aparecia como "recuperei o que você tinha escrito e ainda não estava salvo", sem a família ter editado nada. O autosave de rascunho agora só liga depois que o carregamento assenta (`setTimeout(0)` num ref).

**Cadastro do responsável por CPF (modo teste apenas)** — campo CPF + "Consultar CPF" (`POST /api/admin/consultar-cpf`, provedor `cpfcnpj.com.br`, token de teste) preenche Nome automaticamente; CPF nunca é persistido. **Existe na Central e no Portal do Parceiro** (2026-07-29 — Rafael notou que faltava no Portal do Parceiro, adicionado igual à Central; rota liberada pra parceiro dono do memorial também, antes era staff-only). **Não é produção ainda** — falta token de produção e resolver IP fixo exigido pelo provedor vs IP de egress dinâmico da Vercel (bloqueio técnico sem solução ainda). Plano com decisões em `docs/RASCUNHO_IDEIAS.md`.

## Página do Memorial (`/homenagem/[slug]`) — como funciona
Pública, sem login, **componente 100% servidor** (zero JS client contínuo na rota — interatividade nova sempre vira ilha `'use client'` isolada, sem `requestAnimationFrame`).

**Seções (lendo direto do Postgres):**
- Hero: foto/monograma, nome, datas, cidade (ícone `<MapPin>`), frase preferida, anel dourado fino + glow radial
- Faixa de presença viva (`FaixaPresencaViva.tsx`) — velas/homenagens/memórias
- Biografia com drop-cap + card lateral "Em poucas palavras" (`ResumoPoucasPalavras.tsx`, derivado de dado real)
- Timeline em espinha vertical com nós dourados
- Galeria (`GaleriaFotos.tsx`) — mosaico A/B alternável, lightbox (setas/teclado), `loading="lazy"`
- Mural de memórias (`MuralMemorias.tsx`) — nome+parentesco+texto+reação de coração via RPC `reagir_memoria(p_id)`
- Condolências — grid + formulário, grava real na tabela `condolencias`
- Acender vela (`AcenderVela.tsx`) — vela principal em CSS puro (gota via `border-radius` assimétrico + `rotate(-45deg)`, técnica calibrada 2026-07-23, regra 20 — **nunca mexer**; a descrição antiga desta linha dizia "vídeo real", corrigido 2026-08-26 depois de ler o código de verdade), migrado 2026-08-26 pro mural de 35 velas votivas em canvas com fundo fotográfico real (`components/public/MuralVelasVotivas.tsx` + `lib/muralVelas.ts`, protótipo aprovado pelo Rafael) — era grade CSS de 45 quadrados iguais sem fundo. Chama "voa" da principal até o slot físico calculado (`obterPosicaoDoSlot`, embaralho fixo seed `20260825` espalha a ordem de acendimento pela parede), RPC `acender_vela(p_slug)`, contador nunca desce. **Sem limite de 1x por visitante** (decisão do Rafael, 2026-07-24 — pode acender quantas vezes quiser); a linha antiga mencionava anti-spam via `localStorage`, que nunca existiu no código, corrigido junto.
- Seletor de tema (`SeletorTema.tsx`) — 3 paletas, demo apenas, não persiste
- Rodapé com logo real

**Especificação de mídia:** 10 fotos (8MB cada) + 4 vídeos (100MB cada) = 480MB, quota 500MB/memorial. Aplicado nas 3 rotas de upload.

**Índices:** `idx_homenagens_slug`, `idx_condolencias_homenagem_id`, `idx_mural_memorias_homenagem_id` — todos com `created_at DESC` onde a query ordena por data.

**Segurança (auditoria Opus 2026-07-23):** RLS de insert público removida de `homenagens` (só staff/parceiro autenticado cria memorial), `CHECK` constraints em `condolencias`/`mural_memorias`/galeria, policy duplicada removida, `search_path` fixado em `buscar_homenagens_publicas`, queries paralelizadas com `Promise.all`.

**Não implementado ainda:** troca de tema persistente, compartilhar, música de fundo (biblioteca curada royalty-free planejada, não construída — família NÃO pode fazer upload de música livre, risco de direito autoral/ECAD), múltiplos vídeos por memorial, links de Privacidade/Termos no rodapé, rate-limit de escrita pública via Route Handler (ver Fase Atual).

### Layout responsivo — feito (2026-07-23)
Mockup gerado no Claude Design (export "Bundled Page") analisado seção a seção — maior parte já batia com a produção (galeria mosaico A/B+lightbox, mural com corações via RPC, timeline em espinha, vela com parede de 45+chama voando). Gap real era só largura/layout: container fixo 720px virou responsivo (520px mobile/1100px desktop, classe `.mem-container`), hero virou linha (foto+texto lado a lado) no desktop em vez de sempre coluna centralizada, seção "Acender uma vela" saiu de dentro do hero e virou seção própria perto do rodapé (posição do mockup). Fonte mantida Georgia — decisão do Rafael, não trocou pro Playfair Display/Source Sans 3 do mockup.

## Convenção de Teste
Toda área de cadastro nova vem com 2 registros fictícios já cadastrados.

## Convenção de Formulários
Ver seção Convenções acima.

## Ordem de construção
1. [x] Auth integrado · 2. [x] CRUD de Parceiros · 3. [x] Módulo de Cemitérios · 4. [x] Portal do Parceiro B2B · 5. [x] CRUD de Memoriais (Central) · 6. [ ] Módulo Financeiro completo · 7. [x] Módulo de Usuários (`/admin/usuarios` — criar, trocar papel, ativar/desativar)

## Sócios — Emails
- Rafael (admin): rvnegocioss@gmail.com
- Pedro (admin): pedro.saraiva@estouonline.com.br
- Ricardo (admin): ricrodalves@gmail.com

## O que está pronto
Landing nova na home (2026-09-22, voltada pra família — busca, cemitérios, dois caminhos pra ter o memorial) · Landing "O Fio da Vida" preservada em `/fio-da-vida` (foi a home entre 2026-08-25 e 2026-09-22) · Central completa (Parceiros/Cemitérios/Memoriais/Mapa/Usuários/Emails/Manual) · Portal do Parceiro B2B completo · Busca pública + sub-landing do parceiro · Portal da Família completo · QR Code automático com pipeline de e-mail pro fornecedor · Rate limit middleware centralizado · Política de Privacidade/Termos · Repositório GitHub `rvnegocioss-cloud/legado-digital-` · CI/CD (lint+typecheck+build em paralelo). Detalhe de cada peça nas seções "como funciona" acima.

## Chatbot IA (LegadoBot) — status
**Planejado (não construído):** entrada por voz, acesso de leitura/escrita direta no banco com guardrail (parte mais sensível, precisa desenho cuidadoso antes), escalonamento pra admin humano.

**Implementado (2026-07-14, só teste/sócios):**
- **Interno** (Central + Portal do Parceiro) — `app/api/legadobot/chat/route.ts` + `components/LegadoBotWidget.tsx`, backend Groq (`llama-3.3-70b-versatile`, gratuito, nuvem). Escopo por papel: staff vê tudo, Parceiro só o próprio `parceiro_id` (reforçado no prompt e no código). Navegação automática via diretiva `AÇÃO: /caminho` extraída da resposta.
- **Público** (landing) — `app/api/legadobot-publico/chat/route.ts` + `components/LegadoBotPublicoWidget.tsx`, sem autenticação, sem acesso a dado real, honesto sobre não ter canal de contato publicado ainda. Navegação limitada a `/busca` e `/parceiro/login`.
- Prompts em `docs/LEGADOBOT_PROMPT.md` (interno) e `docs/LEGADOBOT_PROMPT_PUBLICO.md` (público) — atualizar junto com qualquer mudança na Central/Parceiro (regra 14 acima).

## Ideias em avaliação (backlog não decidido, só registrado)
- **"Book" digital de apresentação pra família** (comercial) — material de apresentação pro parceiro mostrar na venda (vídeo, intenção do projeto). Não construído — falta decidir formato (página/vídeo/PDF) e quem produz.
- **Música gerada com IA (Suno)** — familiar gera música sobre a vida do homenageado. Avaliar pro próximo deploy.
- **Drones + mapeamento do cemitério + navegação tipo Waze até o túmulo** (ideia do Pedro) — relatório técnico completo em `/admin/mapa/drone`. 3 opções de captura de coordenada (GPS celular / GNSS RTK / ortomosaico de drone) e 3 de guiar visitante (link Google Maps / bússola própria / rota turn-by-turn tipo mapa indoor) — nada decidido, Fase 5.
- **Vídeo gerado por IA como fundo da landing** — Adobe Firefly/Canva AI/InVideo AI (nenhum conectado como MCP hoje). Avaliar depois do backlog do Pedro.

## O que NÃO está no MVP
Faturamento e cobrança interna · Pagamento online · App mobile nativo · Integração com cartórios · IA para biografias automáticas · Mapa cemiterial avançado · Marketplace de produtos físicos

## Premissas Técnicas
MVP enxuto mas não descartável · Base preparada pra escala e geolocalização · LGPD desde o início (consentimento, privacidade, trilha de alteração) · ERP externo pra faturamento — Legado Digital só registra fechamento mensal · IA deve propor arquitetura antes de escrever código · Explicar em linguagem simples ao usuário o que está sendo feito e por quê

## Convenções de Código
Componentes em PascalCase · TypeScript strict sempre · Variáveis/funções em camelCase · Pastas em kebab-case · Sempre usar Supabase MCP pra operações no banco · Sempre usar Vercel MCP pra deploy

## Skills instaladas
Log de uso em `docs/USO_SKILLS_MCPS.md`.
- **gstack** — 23 slash commands pra estruturar fluxo como equipe. Overhead desproporcional pra tarefa pequena — avaliar antes de invocar.
- **frontend-design** (Anthropic) — direção de design pra UI pública nova/distintiva.
- **ui-ux-pro-max, ui-styling, design-system, design, brand, banner-design, slides** (nextlevelbuilder) — banco de padrões de UI/UX; `ui-ux-pro-max` tem `scripts/search.py --domain <x>`.

## MCPs Disponíveis
Conectadas: supabase, vercel, memory, sequential-thinking, playwright, Google Calendar/Gmail/Drive, SlidesGPT, Three.js 3D Viewer, HyperFrames (HeyGen).
Pendente de auth: Notion, Canva, Windsor.ai. Com falha de conexão: Context7.

## Comandos Úteis
```bash
npm run dev        # inicia servidor local
npm run build      # build de produção
npm run lint       # verifica erros de estilo/linting
npm run typecheck  # verifica erros de tipo TypeScript
npm run start      # inicia servidor de produção
```

## CI/CD — GitHub Actions
`.github/workflows/ci.yml` — todo push/PR roda 3 jobs em paralelo: Lint (`npm run lint`), TypeScript Check (`tsc --noEmit`), Build (`npm run build`). Cache de npm e `.next/` entre runs. Qualquer job falhando = workflow vermelho.

**Branch Protection recomendado (ação manual do Rafael no GitHub):** Settings → Branches → aplicar ao `main` → "Require status checks to pass before merging" → adicionar `ci/lint`, `ci/typecheck`, `ci/build`.

## Credenciais (apenas referência — nunca commitar)
- Supabase Project Ref: yegvazxycfrbhblyzvhg
- Deploy: Vercel (projeto legado-digital-)

---

## Session Start Protocol ⚡

**MANDATORY** at start of each session:
```bash
✓ .claude/COMMON_MISTAKES.md      # ⚠️ CRITICAL - Read FIRST
✓ .claude/QUICK_START.md          # Essential commands
✓ .claude/ARCHITECTURE_MAP.md     # File locations
```

**At task completion:** criar doc em `.claude/completions/YYYY-MM-DD-task-name.md`, mover arquivo de sessão pra `.claude/sessions/archive/` (se criado).

**⚠️ NEVER auto-load:** `.claude/completions/`, `.claude/sessions/`, `docs/archive/` (custo zero de token, só ler se pedido).

---

**Last Updated**: 2026-08-27
**Histórico completo**: `Projects/Legado Digital - Historico Detalhado.md` (vault)

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
