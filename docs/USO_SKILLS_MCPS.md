# Uso de Skills e MCPs — Log

Registro de toda vez que uma skill ou MCP é usada no projeto, motivo e resultado. Ver regra em `CLAUDE.md`.

## 2026-07-13

- **Supabase MCP** (`execute_sql`) — diagnosticar bug "busca não acha nomes". Rodei `select * from buscar_homenagens_publicas('maria', null)` e checagem de `has_function_privilege`/`has_table_privilege` pro `anon` direto no banco de produção. Resultado: função e permissões corretas — confirmou que o bug estava no client (`BuscaMemorial.tsx` ignorava o `error` do `.rpc()`), não no Postgres.
- **redesign-skill** — redesenhar `/parceiros/[slug]` (estava "fraca, faltando elementos"). Auditoria de padrões genéricos (evitar 3 cards iguais, símbolos clichê, fontes/hierarquia fraca) aplicada na reestruturação da página: busca subiu pro hero, seção de conceito virou bloco assimétrico texto+lista de recursos, "Como funciona" virou numeração serif em vez de ícones em círculo.

## 2026-07-29

- **Agent tool, `subagent_type:"Plan"` + `model:"opus"`** — planejar a revisão de layout da ficha do memorial no Portal do Parceiro (Rafael reclamou que o template estava "mobile first", campos difíceis de achar). Instruído a ler os dois arquivos (`app/parceiro/memoriais/[id]/page.tsx` e o equivalente da Central) e pesquisar referência real de formulário denso pra operador antes de propor layout (regra 6). Pesquisou e citou Salt Design System, Attio, HubSpot, Linear, Stripe Apps, Airtable, Notion — achou a causa raiz literal (`grid grid-cols-2` sem breakpoint) e mediu contraste de label reprovando AA (`zinc-500` = 2,58:1). Produziu diagnóstico + proposta de grid 12 colunas + sistema de espaçamento/tipografia + lista de arquivos a mudar. Plano usado quase integralmente na implementação (Sonnet, mesma sessão).
- **Supabase MCP** (`apply_migration`, `list_tables`) — adicionar `homenagens.familia_nome_responsavel`/`familia_telefone` (pedido separado do Rafael: cadastro do responsável precisa capturar nome+telefone, não só e-mail). `list_tables` confirmou schema atual (colunas existentes, sem telefone) antes de migrar.
- **Agent tool, `subagent_type:"Plan"` + `model:"opus"`** — auditoria de segurança real (não superficial) dos 3 níveis de acesso (Central/Parceiro/Família), pedido urgente do Rafael. Instruído a ler RLS/policies reais via Supabase MCP (`execute_sql` contra `pg_policies`), API routes, sessão da família — não aceitar hipótese sem confirmar rodando query de verdade. Achou 1 crítico real (views `parceiros_publicos`/`homenagens_busca_publica` sem `security_invoker`, escrita liberada pra `anon`) + 8 achados médios/baixos, e também confirmou por escrito onde NÃO tinha problema (com a query que provou). Relatório longo, mas todos os achados eram reais e viraram fix de verdade — nenhum falso positivo.
- **Supabase MCP** (`apply_migration`, `execute_sql`) — usado extensivamente pra corrigir os 9 achados da auditoria acima: aplicar cada migration e IMEDIATAMENTE testar com `set local role anon`/`set local role authenticated` simulando o acesso real antes de considerar corrigido. Pegou um erro meu no processo (a primeira tentativa de policy pra `parceiros_publicos` vazava email/cnpj/telefone pra qualquer usuário logado, não só corrigia o problema original) — corrigido no mesmo pente antes de virar risco, graças a testar cada mudança como o role real em vez de só aplicar e seguir.

## 2026-07-30

- **Agent tool, `subagent_type:"Plan"` + `model:"opus"`** — plano de verificação sistemática pedido pelo Rafael depois de 4 bugs achados só testando ao vivo (campo faltando, dado placeholder vazando por e-mail, estado desatualizado invalidando trava de concorrência). Instruído a propor checklist reutilizável de 4 categorias E rodar a varredura de verdade no código agora, não só teorizar. Achou 13 achados médios/críticos + 5 no dashboard, incluindo 1 crítico com risco real de perda de dado (slug de rascunho nunca virava definitivo, memorial de teste do Rafael a horas de ser apagado pela limpeza automática) — corrigido na hora, na mesma sessão.

## 2026-08-04 — Mapa do cemitério com ortomosaico real de drone

- **Agent tool, `subagent_type:"Plan"` + `model:"opus"`** — plano de arquitetura pro pipeline de conversão do ortomosaico (GeoPackage bruto → PMTiles) e as 2 telas de consumo (Central + "Como Chegar"). Instruído a explorar o schema real, rodar `rasterio` de verdade contra o `.gpkg` recebido pra medir tempo/tamanho (não estimar), e testar Range request contra o Supabase Storage real. Achou um bloqueante crítico de dado antes de qualquer código: nenhum cemitério cadastrado ficava perto de onde o drone voou (mais próximo, ~50km de distância) — impediu salvar o mapa amarrado no lugar errado. Entregou pipeline com código Python quase pronto pra copiar (matemática de alinhamento de grade, schema MBTiles, flip de eixo TMS) e 2 arquiteturas de tela detalhadas.
- **Supabase MCP** (`execute_sql`, `apply_migration`) — usado o dia inteiro: inspeção de schema real antes de migrar (`information_schema.columns`, `pg_get_constraintdef`), criação do cemitério novo (`Cemitério Municipal José Lázaro`, Tupaciguara/MG — pesquisado via WebSearch, confirmado pelo Rafael), criação do bucket `mapas` no Storage, migration de schema (`cemiterios.ortomosaico_*`, `lapides.coordenada_origem`), recriação da RPC `obter_localizacao_memorial` com teste real de `has_function_privilege` como `anon`/`authenticated` depois — confirmado que o `revoke` sobreviveu ao `drop`+`create` (risco já documentado nesta sessão).
- **WebSearch** — 2 pesquisas técnicas reais: (1) formato ideal pra hospedar raster tile pyramid em storage estático sem servidor de tiles — confirmou PMTiles como padrão atual, com suporte oficial do MapLibre GL JS via plugin (fontes: docs.protomaps.com, maplibre.org, github.com/maplibre/maplibre-agent-skills); (2) identificação do cemitério real de Tupaciguara/MG "em frente ao Fórum" — achou endereço exato (Cemitério Municipal José Lázaro, Rua Padre Simão Janet, mesma rua do Fórum Adolpho Fidélis dos Santos) antes de cadastrar no banco.
- **Bash (Python: rasterio, pmtiles, Pillow) + go-pmtiles CLI** — sem GDAL/compilador C++ instalado na máquina: `pip install rasterio` (traz GDAL 3.10.3 bundlado, sem precisar compilar nada) resolveu a reprojeção; `pip install pmtiles` deu a função `mbtiles_to_pmtiles()` em Python puro, então o binário `go-pmtiles.exe` (baixado da release oficial do GitHub) acabou nem sendo necessário no pipeline final, só serviu de plano B verificado. Rodado de ponta a ponta contra o `.gpkg` real: ~5min pra gerar a pirâmide completa (zoom 14-22), tile extraído do centro e inspecionado visualmente (não só por header) antes de confirmar qualidade.

## 2026-08-07 — Ruas internas do cemitério + rota real até o túmulo

- **Agent tool, `subagent_type:"Plan"` + `model:"opus"`** — pedido do Rafael pra corrigir a rota do "Como Chegar" (hoje linha reta ignorando caminho interno de verdade), autorização explícita pra Sonnet executar direto o plano sem pedir aprovação de novo ("chama ele planeja e você executa"). Instruído a reaproveitar o mesmo sistema de desenho de linha já usado pra fileira, e a citar linha exata dos arquivos protegidos (regra 17, `GuiaTumulo.tsx`) antes de qualquer edição. Entregou: schema aditivo (`ruas_cemiterio` + RPC `obter_rede_ruas_memorial`), algoritmo de rede/rota em `lib/rotaCemiterio.ts` (noding automático por interseção/encosto em T + Dijkstra, puro TS) com fallback garantido pra linha reta, componente `PainelRuas.tsx` novo, e o diff mínimo exato (3-4 linhas) pro lado público. Plano seguido à risca — nenhuma decisão de arquitetura tomada sozinho por Sonnet.
- **Supabase MCP** (`apply_migration`) — `ruas_cemiterio` (RLS staff-all/parceiro-select via `pode_ver_cemiterio` já existente) + RPC `obter_rede_ruas_memorial` (revoke/grant reaplicado igual sempre, só `service_role` executa) + `obter_geojson_cemiterio` recriada com a chave `ruas` (corpo copiado literal via `pg_get_functiondef` antes de editar, não reconstruído de memória).
- **Bash (`npx tsx`)** — script de teste temporário (`scripts/_teste_rota_temp.ts`, apagado depois) rodou 5 cenários sintéticos (encosto em T, rede desconectada, sem rua, portão/túmulo longe demais) contra `lib/rotaCemiterio.ts` antes de integrar na UI — confirmou a distância pela rede maior que a linha reta (contorna o cruzamento) e todos os `motivo` de fallback corretos.
